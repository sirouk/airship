# Canon reconciliation — 2026-08-03

> **Archived:** moved under `docs/archive/` during the provider-neutral simplification. This file is historical context only and is not normative. See [`../../SIMPLIFICATION.md`](../../SIMPLIFICATION.md) and the living docs in `docs/` for the current contract.

Reconciled against `3f1139308b9fd665b6bcf26502b4d1ea32fbb8de`, the first
tree where the full suite ran green on a clean runner (184/184 discovered and
executed on desktop and mobile, 0 failed, cardinality reconciling on both
gates).

Method: every claim and every document was checked against the code and its
tests. Nothing here rests on a document's own assertion about itself. Where a
document and the tree disagreed, the tree won.

## What this pass changed

`docs/CANON.md` was corrected in 17 places. 133 claims were examined across its
six substantive section groups; 40 were inaccurate. The recurring defect was a
contract that exists as a tested library but has no product path, written in the
present indicative as though a user could reach it — the attested trust tier,
the strict endpoint-proof floor, and remote compute placement all read that way.
See the `canon:` commit for the full list and its evidence.

## Document classification

All 85 documents under `docs/`, excluding `CANON.md` itself.

- **normative** — governs a live technical contract; still authoritative
- **evidence-audit** — the record of a review, audit or measurement
- **reference** — notes on an external API, service or protocol
- **aspirational** — describes intent that is not built
- **historical** — superseded, preserved for provenance

Nothing is deleted. Classification orients; it does not prune.

### normative (33)

| document | purpose | findings still open |
|---|---|---|
| `/Users/chrisk/airship/docs/BROWSER_EXECUTION_PACKS.md` | Normative contract for the browser execution capability broker: which runtimes ship, their exact bounds (source size, time budget, memory, streams), t | — |
| `/Users/chrisk/airship/docs/CONTEXT_FABRIC.md` | Normative retrieval architecture: the expert-page routing decision, the eight-step query path with the executable slice marked, turn-history compressi | — |
| `/Users/chrisk/airship/docs/LOCAL_DEVICE_VAULT.md` | Normative contract for the browser-origin Vault: the OPFS/Web-Locks-then-IndexedDB opener ladder with no page-memory fallback, per-object AEAD custody | — |
| `/Users/chrisk/airship/docs/RELEASE_GATE.md` | Governs the deterministic static release gate: the blocking checks, the enforced per-class artifact budgets, the unsigned deterministic manifest, and  | — |
| `/Users/chrisk/airship/docs/STORAGE_CONFORMANCE.md` | Self-declared normative contract every primary Airship store must pass: correctness (CAS/ETag/412-vs-409), selective retrieval, browser authorization, | — |
| `docs/ACCESS_AND_COMMERCE.md` | Separates identity / Airship entitlement / Chutes credential / attestation as four independent authorities, fixes the rule that payment may never gate | — |
| `docs/ARCHITECTURE.md` | Detailed runtime topology, narrow-waist ports, turn lifecycle, state model, Git and prepared-remote-effect contracts. | — |
| `docs/ATTESTATION_RECEIPTS.md` | Defines the receipt/badge/proof-level vocabulary, the Chutes endpoint-evidence acquisition flow, the portable receipt v1 wire shape, the receipt impor | — |
| `docs/BROWSER_GIT.md` | Wire-level contract for browser-native Git: .git byte layout, base64 worktree envelope, catalog path, implemented verb set, remote/CSP gate, and migra | **yes** |
| `docs/CHUTES_ATTESTATION_EVIDENCE.md` | Endpoint-evidence semantics: what a Chutes TDX quote locally binds, what the browser DCAP QVL verifies, and the claim-scoping rules the Proof UI must  | **yes** |
| `docs/CLIENT_STORAGE_ACCELERATION.md` | Defines the non-authoritative OPFS/IndexedDB ciphertext cache: backend selection ladder, the asymmetric authority rules that keep it out of the CAS pa | — |
| `docs/COMPUTE_CONTINUUM.md` | The normative placement and remote-job contract: pre-spawn placement, the ten browser-owns-authority invariants, and the structural remote-transcript  | — |
| `docs/DESIGN_DIRECTION.md` | Executable design specification resolving ten conflicts across seven lane reviews into one scope rule, one disclosure ladder, one Seal family, and one | **yes** |
| `docs/DESIGN_LANGUAGE.md` | The visual system: character, material palette, performance-as-quality rules, proof-seal grammar, and the token vocabulary that src/ui/tokens.css impl | — |
| `docs/EDGE_PORTABILITY_ACCEPTANCE.md` | Defines the five-project browser/device portability gate and the deliberate limits of emulation-based acceptance. | — |
| `docs/EXTENSION_BRIDGE.md` | Specifies the optional Companion extension: why CORS forces it, the postMessage/long-lived-port transport, the compiled-in destination allowlist, the  | — |
| `docs/GOOGLE_DRIVE_VAULT.md` | Contract for the Google Drive vault adapter: folder hierarchy, drive.file scope, GIS browser token flow, key custody, range/resumable transfer, and de | **yes** |
| `docs/INFERENCE_PROVIDER_REGISTRY.md` | Provider connection/model/session pinning contract, the public-PKCE gate, credential generations, and an explicit statement of which registry code is  | — |
| `docs/KEY_CUSTODY_AND_DEVICE_ENROLLMENT.md` | Separates the three custody authorities (storage grant / key custodian / job grant), records what is implemented today, and specifies the target key g | — |
| `docs/LINEAGE.md` | The clean-room provenance contract: which external repositories informed Airship, the absolute no-source-reuse boundaries (claw-code/claude-code-rs, O | — |
| `docs/LOCAL_FULL_SYSTEM_LAB.md` | Operator runbook for the reproducible local lab: start/status/stop, loopback MinIO + confidential OAuth bridge, the product exercise script, and the n | — |
| `docs/LOCAL_MODEL_PROVIDERS.md` | Subsystem contract for the browser-direct Ollama / LM Studio adapters: public exports from src/inference/local/index.ts, the loopback origin policy, d | — |
| `docs/MEMORY_CONTEXT_SCOPE.md` | Defines the memory/context scope contract: memory.json schema version 2 and its profile binding, the legacy-unscoped admission rule, profile-bound for | — |
| `docs/MEMORY_RELATIONSHIP_GRAPH.md` | Defines the Memory relationship graph module: its extractive-only truth model (no inferred semantics), its node/edge kinds, its device limits, and its | — |
| `docs/PRODUCT_SPEC.md` | Detailed product contract: required properties, capability tiers, performance and scale objectives, compliance posture, and explicit browser-tier non- | — |
| `docs/PROTOCOLS.md` | The canonical wire contracts: event envelope, session manifest v1/v2 semantics, inference transport, encrypted object envelope v1, object store, works | — |
| `docs/PROVIDER_FABRIC.md` | The provider-neutral inference fabric contract: six invariants separating provider/connection/credential/model/transport identity, the page-memory cre | — |
| `docs/SEMANTIC_EMBEDDING_PACK.md` | Runtime contract for the opt-in client-side semantic embedding engine: exact library/model/revision pins, the WebGPU-then-WASM two-attempt rung, and t | — |
| `docs/SESSION_LIBRARY.md` | Module map and host-integration contract for the session library: bounded materialization, pin extraction, fork creation, and the exact language local | — |
| `docs/SOURCE_CONTROL_WORKBENCH_INVENTORY.md` | The WKS-05 removal gate: the enumerated inventory of every control the retired top-level Sources mode must retain inside Workspace → Source Control, p | — |
| `docs/TERMINAL_ENGINE_ARCHITECTURE.md` | Layer separation for the browser terminal (renderer vs interpreter vs runtime vs filesystem), the refresh-reconstruction contract, and the evidence ga | **yes** |
| `docs/THREAT_MODEL.md` | Assets, trust boundaries, posture vocabulary, the principal-threat/control/residual-risk table, key hierarchy, remote-execution boundary, Chutes E2EE  | — |
| `docs/VAULT_COMPOSITION.md` | The evidence-gated composition root for cloud-authoritative encrypted state: coordinator state machine, production wiring, disposable probe lifecycle, | — |

### evidence-audit (41)

| document | purpose | findings still open |
|---|---|---|
| `/Users/chrisk/airship/docs/FERRARI_AUDIT.md` | Launch-gate code-evidence audit: executive verdict, capability scorecard, P0/P1/P2 priority tables with hard acceptance gates, eleven detailed finding | **yes** |
| `/Users/chrisk/airship/docs/VOICE_REVIEW_DISTILLED_2026-07-28.md` | Source-faithful telegraphic distillation of the product-shaping voice review: core theses, how the system should fit together, and section-by-section  | **yes** |
| `/Users/chrisk/airship/docs/audit/PASS1_FINDINGS.md` | Immutable discovery ledger of the whole-system Pass 1 audit: 139 reviewer-validated findings across a status table plus per-finding detail with verdic | **yes** |
| `/Users/chrisk/airship/docs/design-review/lead-critique.md` | Self-directed design critique with rendered-DOM measurements across ten routes, arguing that Airship's clutter comes from a missing type ramp and a mi | **yes** |
| `/Users/chrisk/airship/docs/gap-audit/context.md` | Reviewer-corrected gap audit of the Hermes-level context engine: eleven findings on compression, token estimation, semantic pack delivery, memory reca | **yes** |
| `/Users/chrisk/airship/docs/gap-audit/remediation/inference-fabric.md` | Independent verifier report on the inference-fabric remediation: an honest=True verdict with empirical baseline reproduction, plus seven issues rangin | **yes** |
| `/Users/chrisk/airship/docs/gap-audit/ui.md` | Reviewer-corrected gap audit of interface quality, polish and honesty: a verdict crediting most of the eight UX constraints as met and test-enforced,  | **yes** |
| `docs/ADVERSARIAL_SYSTEM_REVIEW.md` | Independent three-reviewer adversarial code-evidence review with a P0 release-blocker ledger, attack matrix, and validation record. | **yes** |
| `docs/HUMAN_REVIEW_2026-07-25.md` | Human-perspective punch list: 7 reviewer lanes, 84 raw findings deduped to 66 (14 blockers, 29 major, 13 minor), each observed in the running app on a | **yes** |
| `docs/MASTER_PROMPT_ACCEPTANCE.md` | Row-by-row ledger recording which master-prompt requirements the code can prove today, with evidence links and named closure blockers. | **yes** |
| `docs/PRODUCTION_READINESS.md` | Separates repository state from the account-bound external release work (store uploads, Apple packaging, OAuth registration, real-device signoff) and  | **yes** |
| `docs/VISUALIZE.md` | A capture script: every route, moment and transitional state a person can meet, in first-encounter order, written to be photographed. It states outrig | — |
| `docs/VOICE_REVIEW_BACKLOG_2026-07-28.md` | The source-faithful directive register from the 2026-07-28 voice review: ~14 themed tables of requested behaviour vs then-current status, seven delive | **yes** |
| `docs/audit/JOURNEY_ATLAS.md` | The pinned human-journey audit: 12 personas driving the production build at 1440x900 and 390x844, 152 findings across an 8-lane, 12-link chain, with e | **yes** |
| `docs/audit/JOURNEY_CLOSEOUT.md` | Closeout record of the human-journey audit: what the pass changed, what gate now enforces it, what measurement lessons it learned, and what it deliber | **yes** |
| `docs/audit/JOURNEY_METHOD.md` | The corrected method for running a human-journey pass, written as a post-mortem of four structural leaks in the previous pass (hand-picked batches, fi | **yes** |
| `docs/audit/JOURNEY_ROUTING.md` | Generated ownership index proving every one of the 152 human-journey findings sits in exactly one of eight lanes; the gate that regenerates and verifi | **yes** |
| `docs/audit/PASS3_FINDINGS.md` | Record of the pass-3 security/resilience/adversarial-load sweep: two defects found and fixed, the axes swept and how, what was deliberately excluded,  | **yes** |
| `docs/design-review/VISUAL_BUILD_LIST.md` | Adjudicated design build list: verifies the swarm's censuses against the running tree, resolves inter-critic conflicts under four contract guards, and | **yes** |
| `docs/design-review/devils-advocate.md` | Adversarial review of an 84-item design proposal set: which proposals lose information, which regress honesty, which contradict each other, which are  | **yes** |
| `docs/design-review/journey-complaints.md` | Four adversarial persona journeys (phone-only, cold arrival, privacy-sceptic, power user) driven live with Playwright at named viewports against local | **yes** |
| `docs/design-review/lead-observations.md` | Design lead's six measured observations taken from a real 1440x900 connected-turn capture, used as the empirical input to the DESIGN_DIRECTION program | **yes** |
| `docs/design-review/screen-reviews.md` | Six-lane screen-by-screen design review of every remaining route (Source Control + Terminal, Workspace/Editor, Vault, Memory, Proof, Profiles/Account/ | **yes** |
| `docs/design-review/vault-adoption-finding.md` | Localized pre-existing defect: the local-lab vault reached a green 'Contract verified' phase while the runtime never adopted it, so a durability claim | **yes** |
| `docs/design-review/visual-critique.md` | A state-of-the-art visual critique organized by discipline — clarity/copy, mobile, craft/typography — with per-finding severity tags and measured coun | **yes** |
| `docs/gap-audit/accel.md` | Audit of whether detected browser accelerators (WebGPU, OPFS, WASM tier, worker concurrency, WebNN, service worker) are actually leveraged or merely r | **yes** |
| `docs/gap-audit/git.md` | Dimension audit of real Git and the virtual workspace: eleven findings spanning a dishonest capability claim, a live correctness bug, scale collapse,  | **yes** |
| `docs/gap-audit/inference.md` | Adversarial gap audit of the provider layer and agent model-awareness: six numbered findings, each with evidence, impact and a corrected fix plan that | **yes** |
| `docs/gap-audit/remediation/accelerator-leverage.md` | Independent verifier report on the accelerator-leverage package: verdict HONEST, plus five routed issues centred on an unmeasured WebGPU power-prefere | **yes** |
| `docs/gap-audit/remediation/context-engine.md` | An independent verifier's report on the context-engine remediation package: what it confirmed as genuinely built, which checks it re-ran, and nine iss | **yes** |
| `docs/gap-audit/remediation/git-verbs.md` | Independent verifier report on the git-verbs remediation package: confirms the claimed verbs and bug fixes are genuinely implemented (honest=True) and | **yes** |
| `docs/gap-audit/remediation/ui-polish.md` | Independent verifier report on the ui-polish remediation package: confirms the agent's self-reported checks reproduce, then lists seven defects the pa | **yes** |
| `docs/gap-audit/remediation/vault-storage.md` | Verifier report on the vault/storage remediation package: what it independently re-ran and confirmed, the two overreaches that made it honest=False, a | **yes** |
| `docs/gap-audit/remediation/wasi-artifact-channel.md` | Independent verifier report on the wasi-artifact-channel package: verdict HONEST AND SUBSTANTIVE, with a reproduced HEAD control run, plus seven issue | **yes** |
| `docs/gap-audit/shell.md` | A capability audit of the multi-language shell surface (Bash, Python, Rust, JS, WASI) with a live-probe verdict on the WASI Preview 1 runner and ten n | **yes** |
| `docs/gap-audit/tool.md` | Audit of the model-facing tool surface against Claude Code / Codex parity: 30 tools reviewed, seven findings on shell access, search paging, sub-agent | **yes** |
| `docs/gap-audit/vault.md` | Dimension audit of vault, storage, encryption and streaming: eight findings covering a dead default provider, reload amnesia, write amplification, unb | **yes** |
| `docs/gap-audit/wave2/browser-extension.md` | Verifier report on the companion browser extension: mutation-tested security boundary, seven security findings and ten issues, with two confirmed TOCT | **yes** |
| `docs/gap-audit/wave2/connect-inference-ux.md` | Independent verifier report on the connect-inference-ux package: verdict honest=False on one material overclaim, plus a 12-point extension/bridge secu | **yes** |
| `docs/gap-audit/wave2/page-side-bridge.md` | An independent verifier's report on the page-side extension-bridge package: an honest=True verdict backed by four reverted mutation probes, ten securi | **yes** |
| `docs/gap-audit/wave2/provider-oauth-core.md` | Independent verifier report on the provider-oauth-core package: confirms the three grant shapes, S256-only PKCE, honest expiry states and fail-closed  | **yes** |

### reference (5)

| document | purpose | findings still open |
|---|---|---|
| `/Users/chrisk/airship/docs/ACCOUNT_TELEMETRY.md` | Defines what Airship's Account view reads directly from Chutes (four endpoints, response meaning, live invocation headers), the OAuth scope compatibil | — |
| `/Users/chrisk/airship/docs/MODEL_DISCOVERY.md` | Notes on the Chutes browser-callable model surfaces (/v1/models, /chutes, /chutes/{id}/evidence, /e2e/*), their exact envelopes and fields, which sour | — |
| `docs/AWS_S3_REFERENCE.md` | Notes on the AWS Cognito Identity / S3 SigV4 public-client deployment Airship's S3 adapter talks to: the two unsigned Cognito calls, the IAM trust and | — |
| `docs/OPENWEBUI_CLEANROOM_CHAT.md` | Clean-room behavioral study of Open WebUI: which interaction contracts were observed, how each maps onto an Airship-native invariant, and the licensin | — |
| `docs/SHELBY_INTEGRATION.md` | Notes on the external Shelby storage protocol: what Airship would need from it, current public gaps, two candidate integration paths (S3-complete gate | — |

### aspirational (3)

| document | purpose | findings still open |
|---|---|---|
| `docs/ROADMAP.md` | Milestone sequencing M0 through M5 plus the standing go/no-go gates that forbid specific overclaims. | — |
| `docs/WALRUS_STORAGE.md` | Architecture decision record: adopt Walrus as an optional immutable data plane for encrypted blobs/Quilts, and reject WalruS3 as the production S3 com | — |
| `docs/design-review/lane-proposals-surfaces.md` | A measured design review of the rail, composer, model picker, chat chrome, #access, tool-call rendering and the cross-surface design system, with ~45  | **yes** |

### historical (3)

| document | purpose | findings still open |
|---|---|---|
| `docs/AIRSHIP_DESIGN_BLUEPRINT.md` | Consolidated 36-output design record (constitution, IA, chat, workflows, state, performance, QA, delivery sequencing) from the 2026-07-18 design fanou | **yes** |
| `docs/BUILD_PLAN_2026-07-25.md` | Wave-sequenced execution plan turning 70 adversarially-verified gaps into parallel agent packages (W1-W30, C1-C7) with a collision map and a do-not-re | **yes** |
| `docs/EDGE_RUNTIME_CAPABILITY_LADDER.md` | Older implementation doctrine for how "runs anywhere" is expressed as a capability ladder across adapters, persistence, Git, execution, approval modes | — |

## Findings that still look open (48 documents)

Verbatim from the reviewers, each checked against the current tree. Entries
marked UNVERIFIED are exactly that and must not be read as confirmed.

### `/Users/chrisk/airship/docs/FERRARI_AUDIT.md`

P0-01 (Rust kernel as the web turn-loop waist) — STILL OPEN. `grep -rn 'airship-runtime|airship_runtime' src/` returns nothing and crates/airship-runtime/pkg does not exist, so the crate is not wired into the browser loop; docs/CANON.md:1013 still describes the shipped loop as a 'framework-independent TypeScript agent loop'. Note canon lists this in neither Planned nor Explicitly-not-promised, so the P0-01 gate has no canon home. P0-08 (release provenance) — PARTIALLY OPEN: the deterministic unsigned inventory landed (scripts/release-gate.mjs:806 `signed: false`, :2501 'manifest is deterministic and explicitly unsigned'), but signed artifacts, SBOM, and deployed-header/rollback evidence are absent — docs/RELEASE_GATE.md:107-111 and :146-153 concede this, and no SBOM machinery appears in scripts/. P0-06 (per-user Cognito credentials deployed) — UNVERIFIED: src/storage/cognito-identity-credentials.ts exists but grep for 'cognito' across src/ui/*.tsx returns no UI wiring; only inspecting the deployment configuration and running the AWS conformance path would settle whether the credential path is live. P0-02/03/04/05/07 — UNVERIFIED at this depth; each requires running its named fault-injection or negative corpus, which the read-only constraint forbids.

### `/Users/chrisk/airship/docs/VOICE_REVIEW_DISTILLED_2026-07-28.md`

Skill authoring/import (line 60: 'Support discovery through slash commands, adding/importing/creating skills') — STILL OPEN and, worse, unrecorded in canon. `createSkillRevision` exists only in the domain layer (src/profiles/domain.ts:274) with callers confined to tests (src/profiles/domain.test.ts, src/profiles/skill-reconciliation.test.ts); there is no skills view file under src/ui/ (the #skills route exists at src/ui/navigation-model.ts:13, :111 but no *skills-view* module). docs/CANON.md:932-935 describes Skills only as resolution and policy, and neither section 17 Implemented nor Planned mentions authoring or import — so this mandate has fallen out of the ledger entirely. Authenticated GitHub clone/fetch/push and repository export (line 86) — STILL OPEN but honestly recorded, docs/CANON.md:1094-1098 and :1117-1118. Persistent Bash/Linux sandbox with native Git (line 90) — STILL OPEN, docs/CANON.md:1099-1101. Automatic durable profile/session-scoped receipts for terminal/local side effects (line 98) — STILL OPEN, docs/CANON.md:1105-1107. Selectable code themes and syntax highlighting (line 84) — PARTIALLY RESOLVED: src/ui/chat/highlight.ts now exists and is used at src/ui/chat/markdown.tsx:3, :186, and the workspace editor gained a scroll-synced gutter (src/ui/workspace-view.tsx:1932, :1940), but there is no syntax layer in the editor textarea and no code-theme selector.

### `/Users/chrisk/airship/docs/audit/PASS1_FINDINGS.md`

#33 / CAP-02 (skill authoring and import) — CONFIRMED STILL OPEN, matching the document's own admission at PASS1_FINDINGS.md:15-17. No UI authoring surface exists: there is no skills view module under src/ui/, and `createSkillRevision` (src/profiles/domain.ts:274) has no non-test caller. This is the same gap the voice review records. #139 / WKS-03 (language-aware editing) — PARTIALLY RESOLVED beyond the document's admission at PASS1_FINDINGS.md:18-21: the editor gained a presentational scroll-synced line-number gutter (src/ui/workspace-view.tsx:397, :709, :1932, :1940) and chat code blocks gained real tokenization (src/ui/chat/highlight.ts, used at src/ui/chat/markdown.tsx:186), but the editor itself is still a bounded textarea with no syntax layer, so the finding's core remains open. Spot-checks of rows the closeout claims fixed both hold: #13 (Chutes sign-out never revokes the OAuth grant) is resolved — revokeChutesToken now has a production caller at src/ui/app.tsx:8593, :8599 and is regression-pinned by src/ui/connection-continuity.test.ts:46, :56; #11 (no producer for providerInventory) is resolved — src/ui/app.tsx:10515 passes `providerInventory={billingProviderInventory}` into src/ui/billing-view.tsx:310, pinned by src/ui/billing-view.test.ts:119. I did not re-verify the other ~135 findings individually; re-running the focused regression tests the closeout cites, or diffing d161491..HEAD per finding, would settle those.

### `/Users/chrisk/airship/docs/design-review/lead-critique.md`

#10 (the composer is surrounded by things that are not the composer) — CONFIRMED STILL OPEN in its most specific part. The exact 108-character caption the critique quotes still renders as a bare unstyled `<p>` immediately beneath the composer at src/ui/app.tsx:10262-10266: `Encrypted inference through ${connection.model}; this compatibility connection has no required endpoint-proof gate.` The critique's root cause (#0, no type ramp) has structural remediation: src/ui/tokens.css:133-140 now defines an eight-step named ramp (--fs-micro through --fs-hero, each `calc(Nrem * var(--type-scale))`), pinned by src/ui/type-floor.test.ts:12, and src/ui/tokens.css:214-219 defines three named radii (--radius-control, --radius-panel, --radius-chip) with per-theme overrides at :308-319. Whether the *rendered* counts fell from 36/13/32 to systemic values is UNVERIFIED — that requires running scripts/type-ramp-capture.mjs or a headless probe against a build, which the read-only constraint forbids. Findings #1 (no weight hierarchy), #2 (borders doing whitespace's job, '93 elements share 1px rgba(225,217,200,0.106)'), #3 (49 eyebrow labels), #4 (47px serif route H1s), #5 (one route template for documents and tools), #6 (status in five visual languages), #7 (empty states describe absence), #8 (claim rail appears mid-session and narrows the column), #9 (numbers printed without meaning) are all rendered-DOM assertions I could not re-measure: UNVERIFIED. A single headless run of scripts/type-ramp-capture.mjs across the same ten routes at 1440x900, compared against the four numbers in the table at lead-critique.md:8-13, would settle all of them at once.

### `/Users/chrisk/airship/docs/gap-audit/context.md`

#4 semantic-pack-absent-from-production-build — CONFIRMED STILL OPEN, and it is the most consequential one. scripts/semantic-pack-assets.ts still registers the pack only under `configureServer` (:37) and `configurePreviewServer` (:40) with no generateBundle/writeBundle/emitFile hook; dist/ contains no semantic-pack directory (only _headers, assets, execution-packs, extension, favicon.svg, index.html, manifest.webmanifest, release-manifest.json, sw.js); and the base-path half the reviewer added is also untouched — src/indexing/semantic-transformers-loader.ts:9 still hardcodes `const PACK_ROOT = "/semantic-pack/v1/"` instead of deriving it from import.meta.env.BASE_URL. So the only real semantic engine remains unreachable on any deployed static build. #8 no-content-addressed-payload-dedup — STILL OPEN: src/indexing/incremental-indexer.ts:166 still computes chunk id as `sha256(`${file.path}\0${file.revision}\0${chunkIndex}\0${text}`)`, i.e. path- and revision-scoped, so identical text in two files or across revisions still produces two payloads and two embeddings. #10 memory-graph-never-influences-retrieval — STILL OPEN: `grep -rln memory-graph src/` returns only src/deferred-capabilities.ts and src/ui/* rendering modules (memory-view.tsx, memory-controls.tsx, memory-relationships.ts and their tests); no file under src/retrieval/, src/core/ or src/tools/ references it, so the graph still contributes nothing to what the model sees. #9 no-storage-reduction-benchmark-harness — PARTIALLY RESOLVED exactly along the line the reviewer drew: the compression lane now exists (src/core/context-compression-benchmark.test.ts, results published at docs/CONTEXT_FABRIC.md:165-190 against a pinned corpus digest), while the storage-reduction lanes remain absent — there is no bench/ directory and no bench script in package.json, which is consistent since finding 8's lever is still unbuilt. RESOLVED: #1 (planSummaryCompaction at src/core/context-compressor.ts:23, :368; compactionLevel at src/core/context-summary-projection.ts:119, :293); #2 (in-loop projected-token ceiling now evaluated per step at src/core/agent.ts:291-307, after the loop opens); #3 (calibrateBytesPerToken at src/core/context-compressor.ts:234, threaded as bytesPerToken at :215, :291, :304); #5 and #6 (src/retrieval/memory-ranking.ts now exists as the shared ranker, BM25 per src/retrieval/federated-turn-context.ts:23, :55, :141, with the sealed lineage label duly bumped to `bounded-bm25-recent-v1` at :165); #7 (src/tools/context-tools.ts:5, :34, :43 now attaches toolLineage from ../retrieval/tool-lineage, with embeddingProvider/embeddingPosture at :67-68); #11 (src/ui/chat/session-message-presentation.ts:684 handles context.summary.updated).

### `/Users/chrisk/airship/docs/gap-audit/remediation/inference-fabric.md`

Substantively, none of the six actionable issues survives. #1 (validation-ceiling mismatch, 100_000_000 in the catalog vs 8_000_000 in the transport) — RESOLVED: both sides now import one shared constant, src/inference/providers/model-catalog.ts:161 and src/inference/providers/browser-cloud.ts:1021 both bound against MAX_MODEL_OUTPUT_TOKENS, and the boundary the report said was uncovered is now tested at src/inference/fabric.test.ts:395-405. #2 (comment overclaiming a vendor rejection as observed) — RESOLVED: src/inference/providers/browser-cloud.ts:836 now reads 'is one OpenAI or xAI would be entitled to refuse', matching the hedge the report said the docs got right; the 'ceiling is 4096' assertion is gone. #3 (Anthropic turns hard-capped at 4096) — RESOLVED: src/inference/providers/browser-cloud.ts:178 sets DEFAULTS.maxOutputTokens to 64_000, with a comment (:157-177) explaining it is a budget rather than a claimed ceiling and that AnthropicBrowserTransport adopts a 400's stated limit and retries. #4 (doc claims per-provider port partitions the runtime does not enforce) — RESOLVED in the doc's favour: docs/LOCAL_MODEL_PROVIDERS.md:55-59 now states outright 'The allowlist is one flat set, not a per-provider partition — resolveLocalEndpoint takes no provider argument', matching src/inference/local/endpoint-policy.ts:84-95. #5 (shared helper silently accepting a bodyless token response) — RESOLVED: scripts/local-chutes-oauth-bridge.ts:189-191 now gates the empty return on an explicit `allowEmptyBody` flag and otherwise throws 'Chutes token response had no body.' #6 — self-declared stale and now moot: worktreeCommand is defined at src/git/terminal-commands.ts:589 and called at :111. The report's own 'blocking follow-up' is also closed: src/ui/app.tsx:10842-10863 now branches provenance by which field actually supplied the window (`field: "contextTokens"` only when model.contextTokens is defined, `"maxModelTokens"` otherwise) rather than stamping unconditionally, and historyCopied:false is now the canonical, documented truth (src/sessions/session-fork.ts:136, explained at src/ui/sessions-view.tsx:1276, endorsed by docs/CANON.md:1015-1016). #7 was filed as 'NOT A DEFECT, RECORDED FOR THE OWNER' and the behaviour it records is unchanged — src/inference/fabric.ts:480 still rewrites the row to `kind: "manual"`. One narrow residual I could not settle: src/ui/app.tsx:10867-10879 (`contextPolicyForProviderModel`) stamps `source: { kind: "provider-catalog", field: "contextTokens" }` for any InferenceModelDescriptor, including one whose window came from a manual declareModelMetadata call — UNVERIFIED whether a manual declaration can reach that path; tracing whether InferenceModelDescriptor carries the fabric row's source.kind through to this call site would settle it.

### `/Users/chrisk/airship/docs/gap-audit/ui.md`

#8 native-confirm-dialogs — CONFIRMED STILL OPEN, and pinned as acknowledged debt rather than merely overlooked. src/ui/app.tsx still calls window.confirm at three sites: :3959 (profile draft discard), :12596 ('Remove ${selected.name} from the profile manager?'), and :12646 (profile-card switch when dirty). The designed primitive the finding asked for now exists at src/ui/confirm-dialog.tsx and is adopted by terminal-view.tsx:12, vault-view.tsx:8, sessions-view.tsx:20, sources-view.tsx and workspace-view.tsx — but not by ProfileManagerView. src/ui/destructive-confirm-contract.test.ts:20-25 encodes exactly this as `NATIVE_CONFIRM_HOLDOUTS = ["app.tsx", "platform-overlays.tsx"]`, with the comment 'The holdouts below are pinned exactly rather than merely allowed... so does *fixing* one of them without deleting its line here'. So the finding is open, known, and test-guarded against silent growth. RESOLVED: #1 (skip cluster now at src/ui/app.tsx:9563-9591 with 'Skip to conversation'/'Skip to main content'); #2 (src/ui/app.tsx:9796 computes `class={firstRunTranscript && !unrecoveredWork ? "transcript no-turns" : "transcript"}`); #3 (src/ui/chat/highlight.ts exists, imported at src/ui/chat/markdown.tsx:3 and applied at :186); #4 (src/ui/focus-trap.ts extracted, imported by src/ui/approval-dock.tsx:12 and used at :276; approvalPending is folded into platformOverlayOpen at src/ui/app.tsx:2182 and into the shell overlay state at :4679); #5 (src/ui/chat/markdown.tsx:251-252 emits <del> and <em>, with the CommonMark flanking guard the reviewer demanded documented at :263 and encoded at :275); #6 (src/ui/app.tsx:12413 and :12447 gate on `message.role === "assistant" && message.originatingPrompt` with no `message.error` requirement, and :5562 backfills originatingPrompt on resumed rows); #7 (src/ui/app.tsx:11975 now reads 'Secure hardware not checked'; 'page-journal event' no longer appears). The verdict's two 'most serious' items are also resolved: TopbarPostureChip renders unconditionally at src/ui/app.tsx:9619 and is styled without a media query at src/ui/shell.css:119, so the four-axis trust stack is reachable on desktop rather than being dead code below 640px.

### `docs/ADVERSARIAL_SYSTEM_REVIEW.md`

AR-004 (egress consent) STILL OPEN — no taint-aware egress gate exists. src/approvals/ contains broker.ts, consequence.ts, modes.ts, model-reviewer.ts; src/approvals/consequence.ts:46-60 derives consequences only for mutating Git/tool verbs and explicitly maps read-shaped operations (status/diff/log/show) to `null`. The only "egress guard" in the tree is network egress from execution sandboxes (src/tools/execution-tools.ts:278 and :480), not model-context egress. No `.env`/key byte-class gate and no destination/model disclosure for file reads. AR-006 (Rust kernel is the shipped kernel) STILL OPEN — src/load-agent-runtime.ts:9-10 loads `./core/agent` (TypeScript) as the turn loop; crates/airship-runtime/src/kernel.rs (61.6K) exists but is not loaded as the browser's canonical state machine, so the "one versioned kernel ABI in a worker, identical golden traces" exit criterion is unmet. AR-007 (one writer per session epoch) STILL OPEN for sessions — src/ui/tab-presence.tsx:39-56 is an advisory `TabPresenceRoster` (heartbeat/expiry counters), not a lease; src/core/journal.ts:153 has only optimistic CAS fencing. A real writer lease exists but only for terminals (src/terminal/manager.ts:42-45, TERMINAL_LEASE_ROOT / TERMINAL_LEASE_TTL_MS / TERMINAL_LEASE_RENEW_MS), which shows the invariant was built one layer down and never extended to session turns. AR-008 (billing outcome) STILL OPEN — an idempotency key is minted and journaled (src/core/agent.ts:333, src/core/contracts.ts:212) but the Chutes invoke request headers carry only Authorization / Content-Type / X-Chute-Id / X-Instance-Id / X-E2E-Nonce / X-E2E-Path / X-E2E-Stream (src/inference/chutes/transport.ts:310-318) — no idempotency header is transmitted, exactly the gap the finding named. RESOLVED: AR-003 (src/vault/runtime-adoption.ts plus e2e/local-device-app-journey.spec.ts, e2e/local-device-product-journey.spec.ts). AR-005 is substantially resolved — src/tools/schema.ts:18 `compileToolInputSchema` gives compiled schema validation and src/core/agent.ts:784 throws on "a duplicate or reused tool-call operation ID"; UNVERIFIED is only the crash-after-effect half (durable operation ledger surviving a kill-during-write), which docs/audit/PASS3_FINDINGS.md:122-125 also declares outside its sweep because browsers offer no deterministic interruption. Settling it needs a fault-injection harness that kills the tab between effect and journal commit.

### `docs/AIRSHIP_DESIGN_BLUEPRINT.md`

n/a — design record, not a findings ledger. Its section 6 'prioritized backlog / simplification list' is a plan rather than a defect set, and later plans (BUILD_PLAN, VISUAL_BUILD_LIST, JOURNEY_ATLAS) supersede it.

### `docs/BROWSER_GIT.md`

n/a — this is a contract document, not a findings record. Note it now carries the remediation docs/gap-audit/git.md:22 asked for (a CSP subsection stating this build's connect-src cannot reach github.com Smart HTTP), at BROWSER_GIT.md:222-257.

### `docs/BUILD_PLAN_2026-07-25.md`

Not a findings record, but its unexecuted packages are checkable and several are STILL UNBUILT: W13 'Declared capability boundary + web search' (:259) — `grep -rn 'web_search|webSearch' src/tools/` returns nothing; W23 'Sub-agent / task tool' (:439) — `grep -rn 'subagent|spawn_agent' src/tools/` returns nothing; W12 'Expose the jsh shell that already runs' (:409) — airship-sh appears only inside a source comment at src/ui/terminal-view.tsx:549 and the runtime is still not presented as a user choice. LANDED, verified: W6 vault default (src/ui/platform-shell.tsx:463-469), W18 Git history surface (src/ui/sources-view.tsx:80, :519-530), W5 semantic pack in the production build (vite.config.ts:6, scripts/release-gate.mjs:2418), C1 live cloud gates partially (src/inference/providers/browser-cloud.live.test.ts). W16 inference metadata only half-landed — see docs/gap-audit/inference.md below.

### `docs/CHUTES_ATTESTATION_EVIDENCE.md`

n/a — it is a semantics document, not a findings record. Its 'Remaining trust-tier gates' list (:308-319) is a forward gate list, not unresolved review findings, and CANON.md:1110-1116 carries the same items as Planned.

### `docs/DESIGN_DIRECTION.md`

n/a as a findings record, but its decisions verify as built: §Conflict 4 labels stay Title Case; §Conflict 5's amendment landed at e2e/responsive-breakpoints.spec.ts:80-81 (`toBeLessThanOrEqual(92)` plus the harder viewport-share assertion `viewport.height * 0.13`); §Conflict 6 shipped as src/ui/chat/transcript-operations.ts:7 (`DEFAULT_TRANSCRIPT_OPERATIONS = "summary"`) and src/ui/chat/message-parts-view.tsx:469 `OperationStrip`; §Conflict 7's ≥16px seal well is gated by src/ui/design-contract.test.ts:48; §Conflict 10's `<RouteHeader>` exists and is used across billing-view.tsx, capabilities-view.tsx, terminal-view.tsx, context-route.tsx, sources-view.tsx. §Conflict 9's Georgia→Source Serif swap was deferred by the document itself and remains deferred (no font-display/Georgia declaration found in src/ui/styles.css).

### `docs/GOOGLE_DRIVE_VAULT.md`

n/a. Worth crediting: this document is unusually honest about its own unbuilt part — lines 65-70 say plainly "**Not yet reachable from the product.** ...nothing calls `rememberWorkspaceKey` or `adoptCachedWorkspaceKey` yet," which I confirmed (those four exports plus `googleDriveKeyPartition` and `openWorkspaceKeyHandleStore` in src/storage/workspace-key-handle-store.ts:59-246 have zero call sites outside their own test file).

### `docs/HUMAN_REVIEW_2026-07-25.md`

none identified among the blockers I could check, but coverage is partial. RESOLVED with evidence: B3 'nothing tells them where to get a key' -> src/ui/access-view.tsx:107,110 (CHUTES_ACCOUNT_URL, CHUTES_API_KEYS_URL); B7 'TRUST group hidden on ordinary laptop heights' -> src/ui/rail.tsx:25 names the exact defect and :289,:335 implement the height gate; B9 'fixed bottom tab bar treated as free space' -> env(safe-area-inset-bottom) honoured at src/ui/platform-shell.css:7,:229 and src/ui/approval-dock.css:19-20; B11 'default vault dead-ends any deployment not built with a Google client ID' -> src/ui/platform-shell.tsx:463-469 now falls back through isDeployableGoogleOAuthClientId; B13 'phone landscape loads the desktop shell into a 94px slit' -> pinned by src/ui/session-bar-phone-contract.test.ts:11 and src/ui/composer-shell-contract.test.ts:154; B14 'Terminal is a keyboard trap' -> src/ui/terminal-view.tsx:491 and the Escape handler at :633; M1 'TURN COMPLETED.' -> zero occurrences in src/; M9/vocabulary -> src/ui/trust-label-contract.ts:40-59 makes 'Asserted' a typed rung with `requiresReceipt: true`; M11 '#workspace and #editor render byte-identical pages' -> src/ui/navigation-model.ts:105,401 make editor a nested destination. UNVERIFIED: B1, B2, B4, B5, B6, B8, B10, B12 and the 29 MAJOR / 13 MINOR items were not individually checked; most are pixel/aria observations. Settled by building and driving the app at 1440x900 and 390x844 (barred by the READ ONLY rule here).

### `docs/MASTER_PROMPT_ACCEPTANCE.md`

Blockers 2-8 (docs/MASTER_PROMPT_ACCEPTANCE.md:118-141) all still look open, and canon agrees rather than disputes: #3 live Google Drive acceptance against a consented account (CANON.md:1062-1063 keeps Drive Conditional); #4 the benchmark corpus before publishing a 60-78% storage-reduction claim (CANON.md:880-882 still refuses a universal ratio); #5 physical-device and assistive-technology certification (docs/EDGE_PORTABILITY_ACCEPTANCE.md:33-35 names the same gap); #6 remote CPU enclave (CANON.md:1110-1112, Planned); #7 the unified Rust/WASM kernel decision (docs/ARCHITECTURE.md:13-16 still records the split, undecided); #8 live-provider gates (docs/MASTER_PROMPT_ACCEPTANCE.md:32 names src/inference/providers/browser-cloud.live.test.ts as env-gated and unexecuted). Blocker #1 is correctly marked closed-differently by airship-sh. Blocker #2 (whether 'Rust execution' means precompiled WASI or a browser toolchain) is a scope decision nobody has recorded either way — UNVERIFIED as to intent; a decision line in CANON.md §17 would settle it.

### `docs/PRODUCTION_READINESS.md`

By construction, all of them — and correctly so. Every checklist item at :150-177 is account-bound (billing limit, merge to main, Pages repo variables, production OAuth callbacks, Chrome/Edge/Firefox store review, Apple Web Extension Packager + signing, real-device signoff for six platforms, production Chutes PKCE, production Google Drive consent). None can be discharged from source and none shows evidence of discharge in this tree. Canon agrees these are open: CANON.md:1062-1064.

### `docs/TERMINAL_ENGINE_ARCHITECTURE.md`

n/a as a findings record. Its stated WASIX blocker is still the live state: exit semantics and bidirectional workspace mount both fail, activation probes `exit 7` and rejects before registration.

### `docs/VOICE_REVIEW_BACKLOG_2026-07-28.md`

STILL OPEN — CAP-02 ("Users and agents can add/import/install skills, including a built-in skill-creator workflow. **MISSING.**"): still missing. grep for `New skill|Create skill|Add skill|Import skill` across src/ui/ returns zero hits; the catalog is still the fixed set built in src/profiles/catalog.ts:115-122 via createSkillRevision (src/profiles/domain.ts:274), with no creation or import path. STILL OPEN — the register's own "Open — do not treat as done" list of long-lead items is confirmed still planned by canon itself: WKS-08 GitHub authentication and WKS-09 export at CANON.md:1097-1101, TRM-02/03 Bash and native git at CANON.md:1102-1104, TEE-01 at CANON.md:1114-1116, ACC-01/02 provider OAuth at CANON.md:1060-1062. UNVERIFIED — the three browser gates it lists as failing (e2e/vault-auto-adoption.spec.ts:75, e2e/local-device-app-journey.spec.ts, e2e/vault-provider-switch.spec.ts:3). All three files still exist and vault-auto-adoption.spec.ts:89,94 still assert `getByText("Encrypted generation published")`; vault-provider-switch.spec.ts no longer contains the "Connect your Google Drive" string the register blamed, so that assertion was rewritten. Whether they now pass cannot be read from source — running `npx playwright test` on those three files would settle it, which the read-only rule forbids. RESOLVED — PIA-01 and PIA-07, with the file:line evidence above; CAP-04 (boot capability probing) is now started at boot from src/main.tsx:24-26.

### `docs/audit/JOURNEY_ATLAS.md`

The five items docs/audit/JOURNEY_CLOSEOUT.md:177-194 declares open are all still open, confirmed independently: (1) 'app.tsx is 620 KB in one file' — src/ui/app.tsx is 13,046 lines / 642,243 bytes, still undecomposed; (2) '48 of 152 findings are owned and evidenced but not narrated' — still exactly 48, JOURNEY_ATLAS.md:724; (3) the return ledger's 14-day witness is provisional (src/ui/chat/return-ledger.ts + return-ledger.test.ts exist; the 14 days is marked PROVISIONAL in source per CLOSEOUT:129-131); (4) build nondeterminism across checkouts from the dual-imported src/ui/chat/transcript-operations.ts (CLOSEOUT:134-153) — UNVERIFIED that it still reproduces, settled only by two clean clones plus builds, which READ ONLY bars; the ceilings covering both splits rather than the split being pinned is itself evidence it was not fixed; (5) the touch-floor sweep is not whole-product — and the branch tip is literally 3f11393 'journey: the wrap that fixed the overflow broke a touch target', which is live evidence this class is not closed. RESOLVED since the atlas was written: J029/J033 (silent completion announcements, atlas:219,227,248) — src/ui/chat/streaming-slot.tsx:66 now reads 'No announcement lives here any more' and src/ui/chat/message-parts-view.tsx:53 delegates to src/ui/chat/turn-narration.ts (+ turn-narration.test.ts); J027 (Escape drops focus to <body>, 21 Shift+Tabs back, atlas:208) — src/ui/platform-shell.tsx:220,:234-245 now hold an explicit opener ref with a body guard, and src/ui/app.tsx:9601 adds the missing 'Skip to navigation' target.

### `docs/audit/JOURNEY_CLOSEOUT.md`

Four of the five items under 'Open' (:177-194) are confirmed still open in this tree, and one I could not fully settle. (1) **app.tsx decomposition — STILL OPEN and slightly worse.** src/ui/app.tsx is 13,046 lines / 642 KB. Partial extraction did happen (src/ui/chat/composer.tsx and src/ui/chat/session-bar.tsx now exist), but the file did not shrink. :115-116 already predicted this ('`app.tsx` was not touched structurally. Decomposition stays the first dedicated architectural follow-up after merge'). (2) **48 of 152 findings owned but not narrated — STILL OPEN, and now self-consistent.** docs/audit/JOURNEY_ATLAS.md:724 reads '152 findings · 104 narrated · 48 index-only · 8 lanes', exactly matching the closeout's figure; :6 and :31 agree on 152. The gate did what it was built to do — the number no longer drifts — but the gap itself is unclosed. (3) **The 14-day return witness is still provisional — STILL OPEN.** src/ui/chat/return-ledger.ts:107 still carries 'PROVISIONAL. Fourteen days is a guess that has never been checked against how...'. (4) **The build is still nondeterministic across checkouts — STILL OPEN.** scripts/release-gate.mjs:67-70 and :258-261 still document the transcript-operations split ('emitted a 350 B stub in one and a 10.5 KiB chunk in the other') and still cover both splits rather than pinning one, which is the interim the closeout described at :150-153, not a fix. (5) **Touch-floor sweep — UNVERIFIED.** e2e/touch-target-floor.spec.ts exists, but whether its coverage is whole-product or only the controls this pass added cannot be settled by reading; enumerating every interactive element against the spec's selector set, or a coverage run, would settle it.

### `docs/audit/JOURNEY_METHOD.md`

n/a — this is a method document, not a findings record. Its section 5 requirement ("Close with a full re-drive... Only that diff can call a pass complete", :65-69) was met: docs/audit/JOURNEY_CLOSEOUT.md:156-158 records desktop 146 passed / 0 failed / 38 skipped and mobile 92 passed / 0 failed / 92 skipped of 184.

### `docs/audit/JOURNEY_ROUTING.md`

This is a 152-row index and only a handful were fixed. STILL OPEN, from the closeout's own "Open" section and confirmed against the tree: (1) `app.tsx` is not split — src/ui/app.tsx is 642,243 bytes / 13,046 lines, matching "620 KB in one file" (JOURNEY_CLOSEOUT.md:180-182); (2) the build is not deterministic across checkouts — src/ui/chat/transcript-operations.ts is still imported from both a boot-path and a deferred module, and no chunk pin exists (JOURNEY_CLOSEOUT.md:150-165); (3) 48 of 152 findings remain owned but un-narrated. SPOT-CHECKED RESOLVED, so do not assume the rest are open either: J090 (cold open declares the conversation destroyed while restoring it) is fixed by the `durableAuthoritySettled` route fence — src/ui/app.tsx:1764-1778 documents the exact measured race and :2665-2685 now suppresses the loss notice for self-minted and already-reported addresses; J067 (bare `git` dead-ends in jsh) is fixed at src/ui/terminal-view.tsx:313 (`terminalGitHandoffSentence`) and :734, pinned by src/ui/terminal-view.test.ts:481; J064 (completion toast covers the save verdict it announces) is fixed — src/ui/workspace-view.css:495-510 records the 3,632px² and 21,279px² overlaps and makes the notice a flow row instead of an overlay; J080 (sub-16px inputs) is addressed at src/ui/workspace-view.css:418 with the `--fs-field: max(16px, …)` token at src/ui/tokens.css:152. UNVERIFIED — the remaining ~140 rows. Each is a rendered-geometry or live-state measurement against a running build; only re-running the capture (`.ui-capture/` journeys against a preview server) or reading each row's cited source line settles them, and I checked five.

### `docs/audit/PASS3_FINDINGS.md`

none identified — both findings verify as fixed and pinned. Finding 1 (prototype-pollution via own-enumerable poison keys in the profile catalog rebuild): src/profiles/persistence.ts:221-232 now explicitly strips `["__proto__", "prototype", "constructor"]` with the comment "the three keys that walk a prototype if spread blindly," locked by src/profiles/catalog-adversarial.test.ts. Finding 2 (`javascript:alert(1)` accepted as a bare OAuth code): src/ui/connect/authorization-code-paste.ts:165-170 now refuses any `scheme:` prefix and names the one legitimate colon-carrying candidate separately, locked by src/ui/connect/authorization-code-paste-adversarial.test.ts. All four declared regression batteries exist: src/profiles/catalog-adversarial.test.ts, src/workspace/path-adversarial.test.ts, src/ui/connect/authorization-code-paste-adversarial.test.ts, src/inference/local/providers-adversarial.test.ts. The three items at lines 118-129 are declared out of sweep, not open findings; the mid-write crash-forensics exclusion overlaps the unverified half of AR-005.

### `docs/design-review/VISUAL_BUILD_LIST.md`

none identified among the items I could check; this list looks largely executed. RESOLVED with evidence: DN-2/DN-12 (`opacity: .45` cancelling the disabled token) — no `opacity: 0.45` remains anywhere in src/ui/tokens.css; DN-4 ('the streaming caret has never once rendered') — src/ui/chat.css:1042 `animation: stream-caret 1s steps(1, end) infinite` with the keyframes at :1045 and a rationale comment at :1038; DN-5 ('87 `:hover` against 1 `:active`') — 23 `:active` rules now across src/ui/*.css; DN-6 ('popover.css has zero transition and zero animation') — 6 transition/animation declarations now in src/ui/popover.css; DN-7 ('nine names for the one fact') — src/ui/trust-label-contract.ts:40-59 is now a single typed label table keyed by rung with `requiresReceipt`; DN-14 — route-header migration complete as above. UNVERIFIED: DN-1, DN-3, DN-8, DN-9, DN-10, DN-11, DN-13, DN-15 were not individually checked; DN-1 (approval policy inert on phone, unreadable at 360px) and DN-11 (Paper mode dividers invisible) in particular are rendered-pixel claims. Settled by running the built app at 360px and in Paper mode, plus a fresh census for DN-15's 60 font-size literals.

### `docs/design-review/devils-advocate.md`

Mixed, and worth reading item by item rather than as a block — several of its rulings were implemented, so the ones that remain are real. RESOLVED: WP-1 diff correctness (src/ui/sources-view.tsx:988-989,1044 now parse `@@` hunk headers into a `hunk` line kind, the exact fix demanded at :122); the placeholder ruling (src/ui/chat/composer.tsx:30-31 `COMPOSER_PLACEHOLDER = "Message Airship — / for commands"`, with the aria-label untouched at src/ui/app.tsx:10084 as :83 required); 'Delete `established`; keep Verified / **Asserted**' (:35) — src/ui/trust-language.ts:46 returns "Asserted" and :32 records the explicit decision not to soften it to "Recorded", with 'established' surviving only in comments explaining the old defect; the MenuSelect option aria-label a11y fix demanded at :138 (src/ui/menu-select.tsx:183 'Named from contents, not from an `aria-label` that overrode'); the PAGE_SIZE deletion it refused at :17 (src/ui/model-picker.tsx:22-26 keeps `PAGE_SIZE = 30` with a comment citing the same ~5,000px argument); and the 'Authoritative Editor/source-control state' label it said must not ship unbound (:39) — that string no longer appears anywhere in src/ui/. STILL OPEN: **WP-0**, the extraction of StageHeader/Composer/SessionRail out of app.tsx, which it calls the plan's dominant constraint (:71, :120). Composer and SessionBar were extracted; app.tsx grew to 13,046 lines anyway, and docs/audit/JOURNEY_CLOSEOUT.md:115-116 independently confirms the structural work was deferred. UNVERIFIED: the phone terminal keyboard defect (:43, 'Airship's own `textarea.xterm-helper-textarea` (9x23px) never receives focus on tap'). src/ui/terminal-view.tsx:167 now carries a 'Measured: clicking the xterm moves focus to `textarea.xterm-helper-textarea`' comment, which shows the path was investigated, but whether tap-to-focus works on a phone cannot be read out of source — a mobile-project Playwright run asserting focus on that textarea after a tap would settle it.

### `docs/design-review/journey-complaints.md`

MIXED — some blockers verified fixed, three verified STILL OPEN, and the geometry findings are UNVERIFIED.

STILL OPEN 1 — "[blocker] No answer on screen says which model produced it" (:568). The assistant card at src/ui/app.tsx:12333-12343 renders the role word "Airship" and a capability-tier pill reading `Initial · {capabilityTierLabel(capabilityTier)}`; the only other visible identifier is the receipt chip at src/ui/app.tsx:12379, `{message.receipt.receiptId.slice(-8)}`. No model id is rendered on the card in any branch. The related sub-claim ("byte-identical to the label on a local system notice") IS resolved: the welcome pseudo-turn no longer renders as an assistant card (src/ui/chat/transcript-intro.tsx:7-20), local commands now carry a distinct status string (src/ui/app.tsx:5456, "Local command · excluded from model context"), and the tier pill gained a defining tooltip at src/ui/app.tsx:12339. The core complaint — the answer does not name its model — is unresolved.

STILL OPEN 2 — "[major] Identifiers truncate head-8 in one place and tail-8 in another" (:521). Both conventions are still live: tail-8 at src/ui/app.tsx:12379 and src/ui/app.tsx:12774, head-8 at src/ui/chat/session-bar.tsx:328, src/ui/chat/session-bar.tsx:350 and src/ui/proof-view.tsx:737. src/ui/proof-view.tsx:210 uses both inside one expression: `receipt?.receiptId.slice(-8) ?? sessionId?.slice(0, 8)`.

STILL OPEN 3 — the type-scale complaint (:96, "The type scale is the desktop scale verbatim"), see visual-critique for the measured version.

RESOLVED (verified) — "[blocker] Cold first run reports durability as Encrypted Google Drive · cross-device" (:200): src/ui/platform-shell.tsx:471-484 documents the exact defect and its fix ("the row renders selection *plus* state, so a selection can no longer read as an adoption"), the DURABILITY table follows at :493-497, and src/ui/platform-shell.test.ts:330-331 pins the old string so it cannot return. "[blocker] The keyboard-aware rule is dead code" (:12): real rules now exist at src/ui/routes.css:2538 and src/ui/platform-shell.css:295, pinned by five assertions in src/ui/composer-shell-contract.test.ts:111-144. "[blocker] Connection sliced in half at 1440x900" (:212): docs/audit/JOURNEY_CLOSEOUT.md:45 records the src/ui/rail.tsx height gate that stops the self-opening recents list pushing Vault/Connection/Account below the fold.

UNVERIFIED — "[blocker] The terminal cannot be typed into on a phone" (:24). I found coarse-pointer handling only for the tab-rename control (src/ui/terminal-view.css:45) and focus plumbing that targets xterm's helper textarea (src/ui/terminal-view.tsx:862-869), which is a desktop-focus concern. Nothing in source proves or disproves that a tap reaches the shell. Settled by: driving #terminal at 430x932 with a touch-enabled context and asserting document.activeElement lands on textarea.xterm-helper-textarea after a tap. Every remaining pixel-geometry finding in this file (the 4402px connect flow, the 180px tool card, the 215px landscape conversation, the three right-clipping routes, the 424px editor tab bars) is likewise UNVERIFIED from source and needs a re-drive at the viewports named in each finding.

### `docs/design-review/lane-proposals-surfaces.md`

Mixed, and it must be read proposal-by-proposal rather than as a block. STILL OPEN — "Replace Georgia and give the display serif exactly two jobs" (:1127): Georgia is still bound in four places, src/ui/routes.css:310, :721, :1123, :1170. STILL OPEN — "Focus mode (⌘.) that trades chrome for a 28px honesty strip" (:112): there is no `focus` rail state in use; src/ui/app.tsx:2518 lists "focus" only as a command-palette keyword for the collapse toggle, and no ⌘. binding exists. RESOLVED — the three-state rail (:31), with the token/state evidence above. RESOLVED — the design-system consolidation proposals (:964 `<RouteHeader>`, :985 `<Seal>`, :1014 `<Tabs>`, :1037 `<MetricStrip>`): src/ui/route-header.tsx, seal.tsx, tabs.tsx and metric-strip.tsx all exist with their own tests. RESOLVED — "One type ramp: eight tokens" (:931): src/ui/tokens.css:133-140 defines exactly the eight `--fs-*` steps. UNVERIFIED — every proposal whose diagnosis is a pixel measurement of a running build (composer widths, ModelPicker anchor, tool-call rollup geometry, the three contrast failures at :1104). Source inspection cannot settle those; re-measuring against a preview build at the stated viewports would.

### `docs/design-review/lead-observations.md`

Mostly resolved. #1 (tool-call rollup swallows the answer) RESOLVED — src/ui/chat/transcript-operations.ts:7 defaults to `"summary"` and src/ui/chat/message-parts-view.tsx:469 renders an `OperationStrip` rather than stacked full-width cards. #2 (four contradictory trust strings in one viewport) RESOLVED — the vocabulary is centralized in src/ui/trust-language.ts (TURN_EVIDENCE_COPY at :206 maps one state to one seal/chip/line), and the fix is documented at the call sites: src/ui/app.tsx:11846-11847 ("one failure... They are one function now"), src/ui/app.tsx:11910-11911, src/ui/topbar.tsx:21, src/ui/model-control.tsx:196. #5 (composer clipped when connected) RESOLVED — e2e/responsive-breakpoints.spec.ts:80-81 pins composer height and its viewport share, with src/ui/scroll-affordance.ts and the recent journey commits addressing wrap/touch-target fallout. #6 (detached model selector) RESOLVED — src/ui/model-control.tsx:196 records removing the misleading "E2EE · evidence recorded" pill. #3 (claim stack is six identical rows all reading "Asserted · Turn receipt") and #4 (chrome before content, ~230px) are UNVERIFIED: both are rendered-geometry and rendered-copy claims that cannot be settled by reading source. What would settle them: a fresh 1440x900 connected-turn capture, or reading the assertions in e2e/claim-stack-layout.spec.ts to see whether the six-row at-rest collapse and a content-start-offset budget are actually pinned. src/ui/trust-language.ts:122 `claimLanguage(key)` returning distinct `{primary, technical}` pairs per claim suggests #3 was addressed, but that is inference from the data shape, not proof of the rendered rail.

### `docs/design-review/screen-reviews.md`

Mixed — one large finding closed, one still open, one partial. RESOLVED: 'Give Source Control a History pane wired to the log/show/tag/stash/merge/restore/reset verbs that already exist' — src/ui/sources-view.tsx:80 `const [pane, setPane] = useState<"changes" | "history">("changes")`, :519-530 renders a History tab with an explicit unavailable-reason branch ('History unavailable: ...'), and :571 offers 'Read the history of {worktree.branch}'. The `not-a-source-control-mutation` throw the review cited still exists (src/ui/sources-view.tsx:1292) but is now the narrow 'not a mutation this Source Control panel performs' guard beside the new read path, not the reason history is unreachable. STILL OPEN: 'Collapse the 614px Source Control preamble into one 44px repo bar' — the proposed `.git-repo-bar` does not exist (`grep -n 'git-repo-bar|repo-bar' src/ui/sources-view.css src/ui/sources-view.tsx` returns nothing); what shipped instead is a `<details>` disclosure over the trust band (src/ui/sources-view.css:81-103 `.git-sources-trust-disclosure`), a smaller and different fix. The exact reclaimed-pixel figures are UNVERIFIED without running the app. STILL OPEN (terminal lane): 'the route never says "airship-sh" or "POSIX" ... the manager is hardcoded to WebContainer and the runtimes are never presented as a choice' — airship-sh appears in src/ui/terminal-view.tsx only inside a comment at :549, and no runtime chooser is exposed. UNVERIFIED: the Vault (:340), Memory (:549), Proof (:798) and Profiles/Account/Sessions (:968) sections were not checked item by item; settled by driving each route at the three captured viewports.

### `docs/design-review/vault-adoption-finding.md`

RESOLVED, and resolved along both of the candidate paths it offered rather than one. Fix (2), the copy change it said was owed either way: src/ui/vault-view.tsx:869-872 now returns 'Contract verified · not adopted' with the comment 'A verified contract is not an adopted runtime. Saying only "Contract verified" while the workspace is still in page memory is the overclaim this product exists to avoid'; src/ui/vault-view.tsx:876-886 downgrades the seal for that state to "attention" rather than "verified"; src/ui/vault-view.tsx:373 renders 'Storage contract verified · runtime not adopted'; and src/ui/vault-view.test.ts:52,111 pin both the label and the defect it guards. Fix (1), adoption actually following a verified contract for a preference-selected backend: e2e/vault-auto-adoption.spec.ts now exists and asserts that setting `vaultBackend: "local-lab"` (spec :13-17) auto-adopts from both localhost and 127.0.0.1 origins (:44-53) without any user action, which is the behaviour the audit's expectation assumed. The audit spec's own assertion still stands unmodified at e2e/route-adversarial-audit.spec.ts:105 ('Encrypted runtime active'), so it is now asserting a state the flow reaches. Nothing here looks open.

### `docs/design-review/visual-critique.md`

MIXED — one craft blocker verified STILL OPEN with fresh measurements, two mobile blockers verified RESOLVED, the rest UNVERIFIED.

STILL OPEN — "[blocker] The eight-step type ramp is lived in two steps: 83% of all type-token uses are the two smallest sizes, and --fs-hero has zero consumers" (:302). Re-measured against the current tree: --fs-hero appears exactly twice in src/, at its declaration src/ui/tokens.css:140 and in the token inventory list at src/ui/density-contract.test.ts:20 — zero consumers, unchanged. Counting --fs-* occurrences across src/ui/*.css now gives micro 392, caption 273, meta 99, title 29, body 28, lead 27, field 21, display 6, hero 1: the two smallest are 665 of ~876 uses, 76%. The percentage moved four points; the finding did not.

RESOLVED (verified) — "[blocker] The approval-policy menu opens 164px above the top of the viewport on every phone" (:173). The composer control uses MenuSelect's default upward placement (src/ui/menu-select.tsx:20), and the viewport-clamping effect at src/ui/menu-select.tsx:70-92 early-returns for anything but "down" — so the JS path still does not clamp upward menus. But the CSS now overrides placement entirely on phones: src/ui/menu-select.css:54, `@media (max-width:640px), (max-width:950px) and (max-height:500px) { .menu-select-popover, .menu-select.placement-down .menu-select-popover { position: fixed; inset: auto 8px max(8px, env(safe-area-inset-bottom)) 8px; ... max-height: 70dvh; } }` — the popover is pinned to the viewport bottom with a 70dvh cap, so it cannot resolve above the viewport top.

RESOLVED (verified) — "[blocker] At 360px the approval-policy control paints no label — a teal dot and a chevron" (:191). src/ui/chat.css:1803-1814 now fixes the trigger at `width: 18ch`, sized in ch to the longest label ("Auto Approve") with a comment recording that 17ch clipped at phone metrics; only the adjacent `.composer-posture__word` is visually hidden below 380px (src/ui/chat.css:1631-1638).

RESOLVED (verified) — "[blocker] Preferences claims your data is in Encrypted Google Drive while Vault says Disconnected" (:30), the same defect as the journey-complaints blocker: src/ui/platform-shell.tsx:471-484 with the regression pinned at src/ui/platform-shell.test.ts:330-331.

PARTIAL — "[minor] Every assistant message is stamped 'Initial · Browser baseline' with no definition anywhere" (:150). The pill now carries a definition in its title attribute (src/ui/app.tsx:12339: "Initial session observation. ... Tool results name their live producing runtime separately"), and the welcome card that duplicated it is gone (src/ui/chat/transcript-intro.tsx:7-20). It still renders on every assistant message (src/ui/app.tsx:12337-12343), and the definition lives only in a tooltip.

UNVERIFIED — the remaining craft measurements (253 off-scale gap declarations, 115 distinct padding values, 179 line-height literals, the 5.05:1/6.85:1 grey pair, the evidence sidebar set larger than the answer) and every mobile geometry finding (landscape composer under the tab bar, 312px fixed chat chrome, the 350px Workspace header, the "]" conversation title, the 227px terminal). These need the same instrumented re-drive the document itself used: measured DOM rects at 1440x900, 430x932, 360x800 and 932x430, plus a re-run of its CSS-token census. I did not run any of it.

### `docs/gap-audit/accel.md`

RESOLVED — #1 (probe never kicked off at boot, semantic worker latches WASM on WebGPU machines): src/main.tsx:21-26 now fires `getBrowserCapabilityRegistry().refresh()` at boot with the comment "Correctness does not depend on this call: every consumer awaits the registry rather than sampling a cold snapshot", which is the stronger of the two fixes the finding demanded. RESOLVED — #2 (powerPreference computed and never applied): now threaded end to end — src/indexing/semantic-browser-provider.ts:84 supplies `scheduling.powerPreference`, src/indexing/semantic-worker-provider.ts:47,213,304 carries it through the initialize message, and src/indexing/semantic-transformers-loader.ts:25,32,182 maps it via `ortPowerPreference` onto both the pre-flight `requestAdapter` and `onnx.webgpu.powerPreference`. RESOLVED — #4 ("N workers" printed for a pool that does not exist): the number is now explicitly labelled a ceiling, src/tools/browser-capabilities.ts:18 "maxWorkerConcurrency is a derivation ceiling, not a count of running or reserved workers", src/capabilities/runtime-load.ts:6, and the UI comment at src/ui/capabilities-view.tsx:161. RESOLVED — #9 (service-worker/cache-storage report API presence only): src/capabilities/browser-runtime.ts:381-385 now carries `observation.evidence === "probe-passed"` through instead of hardcoding api-exposed, with the comment naming the exact defect. PARTIALLY OPEN — #3 (preferredWasmTier selects no differently-compiled module): the tier now really drives ORT thread count (src/capabilities/browser-runtime.ts:336-338) and the UI says so honestly (src/ui/capabilities-view.tsx:230, class `capability-policy-inert`), but the finding's part-(2) — a second `+simd128` build of the e2ee crate — was not done: grep for `simd128` across scripts/ and crates/ returns nothing, so no differently-compiled artifact is selected by the tier. STILL OPEN (honestly labelled) — #5 preferredWorkspaceStorage and #6 heavyPackLoading still activate nothing; they are now rendered as "observations, not activations" (src/ui/capabilities-view.tsx:234) and fed to the prompt (src/tools/live-environment.ts:182,185), and heavyPackLoading gates only a semantic-mode refusal (src/retrieval/client-context-runtime.ts:117-120, whose own comment says "`heavyPackLoading` gates nothing on its own"). STILL OPEN — #8 (vector search is scalar main-thread JavaScript): src/indexing/flat-index.ts is still 2.3K with `cosine()` at :49 called per chunk from :20; there is no cached norm, no scoring worker, no packed Float32Array, and still no src/indexing/flat-index.test.ts in the directory listing. STILL OPEN — #7 (WebNN probed, used by nothing): no consumer outside the probe, the capabilities view and the prompt projection.

### `docs/gap-audit/git.md`

Seven of eleven resolved; three still open in whole or part. STILL OPEN — #4 `workspace-fs-collapses-at-scale`: the architecture the finding named is unchanged. src/vault/encrypted-workspace.ts:232-235 `loadManifest()` still does a full `store.get(rootCloudKey)` plus whole-manifest decrypt, and it is called on every read (:67), stat (:80), list (:103), write (:185) and delete; `advanceManifest` (:238-241) re-seals and CAS-uploads the entire manifest per write. The OPFS ciphertext tier that shipped cannot help here by design: src/storage/caching-object-store.ts:21-22 states it "Caches only explicitly immutable encrypted objects. Provider reads remain authoritative for mutable heads, listing, create-if-absent, and every CAS," and the workspace head is a mutable head. There is still no directory index. PARTIALLY OPEN — #6 `no-merge-stash-tag-revert-discard`: tag, stash, merge and restore (discard) all landed (src/git/types.ts:15-18, dispatched at src/git/terminal-commands.ts:89-94 and :84), but rebase, cherry-pick and revert remain absent from GIT_CAPABILITIES entirely. PARTIALLY OPEN — #9 `import-drops-binaries-and-cannot-auth`: the silence is fixed (src/tools/repository-import.ts:27,154,203 now count and report `skippedBinary`), but binaries are still skipped and the fetch sends no Authorization header (src/tools/repository-import.ts:135 sends only `Accept`), so private repositories remain unreachable — consistent with CANON.md:1096-1097 listing browser-safe GitHub authentication as planned. RESOLVED — #1 (src/git/validation.ts gained the origin gate; capabilities carry `permittedOrigins`, populated at src/git/workspace-adapter.ts:1689-1690, surfaced at src/ui/sources-view.tsx:1197-1217 and :1340-1369, described to the model at src/tools/git-tools.ts:192, and documented at docs/BROWSER_GIT.md:222-257); #2 (src/git/workspace-fs.ts:286-294 now carries a real `ino` and a `ctimeMs` distinct from `mtimeMs`, with the DIRC/compareStats rationale at :323-328); #3 (src/git/validation.ts:17 `maxPathsPerRequest: 4_096`, enforced at src/git/operations.ts:49,56,169, chunked at src/git/client.ts:147,153,250 via `chunkedPathMutation` at :280 which chains the worktree fence across chunks, and src/tools/git-tools.ts:81 now advertises the matching cap); #5 (log and show implemented — src/git/operations.ts:118,226, src/git/workspace-adapter.ts:701, dispatched at src/git/terminal-commands.ts:86,88, covered by src/git/history.test.ts); #7 (src/git/encrypted-workspace-adapter.ts:396-401 addRemote/setRemoteUrl, `remote-config` capability at src/git/types.ts:19, covered by src/git/remote-config.test.ts); #8 (src/git/terminal-commands.ts:110-111 routes `worktree` to `worktreeCommand`); #10 (src/git/gitignore.test.ts now exists with 6 tests); #11 (src/git/memory-adapter.ts is no longer exported from src/deferred-capabilities.ts and src/startup-boundary.test.ts:70 asserts `reachable.has("git/memory-adapter.ts")` is false — it survives only as a test double).

### `docs/gap-audit/inference.md`

Three still open, one partially, two closed. STILL OPEN — #2 no-mid-conversation-switch (:32): src/sessions/library.ts:61 still declares `historyCopied: false` as a literal type, and `context.transferred`, `forkWithRoute` and `carryContext` have zero occurrences anywhere in src/, so switching model or provider still cannot carry the thread. STILL OPEN — #3 agent-model-metadata-thin (:55) at exactly the hop the audit flagged as 'MISSING HOP the auditor skipped' (:70): src/core/operating-charter.ts:42-46 `InferenceModelPromptDefinition` is still exactly `{id, inputModalities?, features?}`, and the facet emitter at :100-107 still renders only `input=` and `features=`. The `ctx=` facet exists only in `renderInferenceAvailabilityForPrompt` (src/inference/providers/session-route.ts:285), which the audit itself said is test-only — still true, its only references are src/inference/providers/provider-registry.test.ts:25,478,482,512. STILL OPEN — #5 part (1) local CORS remediation (:106-110): `grep -rn OLLAMA_ORIGINS src/ e2e/` returns nothing, so the user is still never told the exact setting to change; part (2) port flexibility IS done (src/inference/local/endpoint-policy.ts:39-52 enumerates twelve loopback origins and docs/LOCAL_MODEL_PROVIDERS.md:61-62 matches). PARTIAL — #1 cloud-no-context-window (:7): the mechanism landed (src/inference/fabric.ts:458-476 `declareModelMetadata`; per-model output resolver at src/inference/providers/browser-cloud.ts:62,:1019; Anthropic's stated ceiling now read from the refusal body at :551, closing the hardcoded-8192 bug at fix step 4), but `declareModelMetadata` has no caller outside its own test (hits are src/inference/fabric.ts:458 and src/inference/fabric.test.ts only), so no operator can declare a window from the product; and fix step 5 'Surface the unknown' is absent — `grep -rln compression src/ui/` matches only src/ui/composer-shell-contract.test.ts. CLOSED — #6 no-oauth-token-revocation: src/auth/chutes-oauth.ts:36,:38,:306 `revokeChutesToken` with both endpoints, covered at src/auth/chutes-oauth.test.ts:423-501. MOSTLY CLOSED — #4 no-live-cloud-provider-gate: the Node wire gate exists (src/inference/providers/browser-cloud.live.test.ts) but the browser-reachability half is missing — no playwright.live-cloud.config.ts among the eight playwright configs and no e2e/live-cloud-providers.spec.ts, so browser CORS for OpenAI/Anthropic/xAI is still asserted rather than proven.

### `docs/gap-audit/remediation/accelerator-leverage.md`

Four of five RESOLVED; one partially open. Issue 1 (the powerPreference downgrade, which the report called 'the most consequential') — RESOLVED and reversed: src/indexing/semantic-transformers-loader.ts:174-182 no longer deletes the pin, and the comment states the reasoning the report demanded verbatim ('Deleting the reviewed pack's own high-performance pin on that non-evidence would silently hand a multi-GPU laptop to its integrated adapter, an unmeasured downgrade, so the pack keeps its value'); src/indexing/semantic-transformers-loader.test.ts:60-65 now asserts 'high-performance' survives a policy that says nothing. Issue 2 (near-tautological byte assertion) — RESOLVED: src/indexing/semantic-transformers-loader.test.ts:90-99 replaced the self-referential reduce with hardcoded reviewed totals and a comment naming the exact defect ('Recomputing the same reduce over the same manifest could not fail for any implementation that sums the same list, which pins nothing'). Issue 3 (inert wasmPaths leaving a CSP-blocked jsdelivr fetch) — RESOLVED: the pin is now live and same-origin (src/indexing/semantic-transformers-loader.ts:358-360 'Points ONNX Runtime at the same-origin pack. Transformers.js installs a `cdn.jsdelivr.net` `wasmPaths` **object**...'), backed by a fail-closed check `unpinnedOrtRuntimeBinaries()` (:349-360) and a bundle-derived binary list (:320-327) so the pin cannot point at a 404; `grep -rn jsdelivr public/_headers index.html` returns nothing, consistent with the CSP never having allowed it. Issue 5 (UI overreach) — RESOLVED: src/ui/capabilities-view.tsx:230 now reads 'would request {n} ONNX Runtime thread(s) the next time the semantic pack loads. This panel does not observe whether that pack is loaded now.' Issue 4 (`maxWorkerConcurrency` reads as a pool size) — PARTIALLY OPEN, and its premise inverted. The rename is still not done: src/capabilities/browser-runtime.ts:94 still declares `maxWorkerConcurrency`. The e2e half the report routed to whoever owns e2e/ is gone (`grep -rn maxWorkerConcurrency e2e/` returns nothing). But the report's claim that 'nothing reaches the model prompt' is now stale in the risk-increasing direction — src/tools/browser-capabilities.test.ts:47 asserts the whole scheduling policy including `maxWorkerConcurrency` IS serialized to the model. The mitigation is explicit rather than accidental: browser-capabilities.test.ts:48 pins the tool description text 'maxWorkerConcurrency is a derivation ceiling, not a count of running or reserved workers', and src/capabilities/browser-runtime.ts:88-93 carries the same warning. So the honesty risk is contract-bound, but the misleading name now reaches the model as well as the type.

### `docs/gap-audit/remediation/context-engine.md`

NONE IDENTIFIED — eight of nine verified resolved in the current tree; one is structurally fixed but its numeric anchor is UNVERIFIED.

(1) "tier-2 compaction never happens ... asserted in the report but not exercised anywhere" — RESOLVED. src/core/context-compaction.test.ts:74 now asserts the observed level SEQUENCE: `expect(committed.map((entry) => entry.compaction.level)).toEqual([1, 2])`, with a comment at :67-71 recording why eight rounds are needed and naming `toBeGreaterThanOrEqual(1)` as the assertion that "passes forever without" catching this. src/core/context-compaction.test.ts:91 asserts compactionLevel 2 and :97-104 exercises the one-level-above-subsumed rejection for levels 1 and 3.

(2) "Published corpus digest does not reproduce" — STRUCTURALLY RESOLVED, numeric anchor UNVERIFIED. The named cause is gone: docs/CONTEXT_FABRIC.md:165-167 now records the corpus as "pinned inside that test file" and :181-184 states the reason explicitly — "an earlier version of this table was measured against live sources, and editing any of those sources silently invalidated it. The digest can now only move when the fixture moves." Whether the new digest at :172 reproduces is UNVERIFIED; settled by running src/core/context-compression-benchmark.test.ts and comparing its printed digest, which the read-only constraint forbade.

(3) "UNDISCLOSED policy hole: the compaction body escapes the session-pinned maxSummaryDeltaBytes" — RESOLVED. src/core/session-audit.ts:1685 now calls `summaryBodiesWithinPolicy(summary, canonical.compression.maxSummaryDeltaBytes)`, imported from context-compressor at src/core/session-audit.ts:37, i.e. the pinned bound now covers bodies and not only the delta.

(4) "compaction.method is a self-assertion with NO provenance at all" — RESOLVED. src/core/context-summary-projection.ts:503-504 enforces the binding both ways: `(provenance !== undefined) !== (candidate.method === "summarizer-port-v1")` and `provenance.responseDigest !== candidate.bodyDigest`, with the rationale at :66-70. The producer side throws rather than defaults: src/core/context-compressor.ts:517-521 rejects a compacted tier without provenance and one whose adapterId does not match.

(5) "The bodyDigest negative test proves less than it claims" — RESOLVED. src/core/context-compaction.test.ts:138 adds "rejects a tier body that no longer matches its bodyDigest even after the commitment is resealed", with :147-149 explaining that resealing is what forces execution to reach the dedicated check.

(6) "The in-loop guard's stated rationale does not match its behavior for tool-using turns" — RESOLVED. src/core/agent.ts:305-312 now derives one `ceilingTokens` that grants a turn opening over the window a fixed OVER_WINDOW_LOOP_ALLOWANCE_TOKENS above where it opened, and src/core/agent.ts:324 computes remainingToolOutputBytes from that same ceiling. The comment at :300-304 states the rule ("One ceiling drives both the refusal and the tool-output budget") and docs/CONTEXT_FABRIC.md:150-158 documents it.

(7) "Weak assertion in the cross-path ranking test" — RESOLVED. src/retrieval/memory-ranking.test.ts:93 now asserts a two-element pinned order, `expect(injected).toEqual(["m-1", "m-3"])`, with :91-92 recording that the ordering is the thing under test.

(8) "Minor overstatement: 'recency prior kept strictly below the gate'" — RESOLVED. src/retrieval/memory-ranking.test.ts:47-49 now states the exact behaviour ("lands it exactly ON the default gate ... excluded only because the gate is exclusive") and asserts the exact value.

(9) "adding the two tool retriever ids to RETRIEVER_IDS widens canonicalLineage ... the comment asserts an invariant the validator does not check" — RESOLVED. src/core/context-selection.ts:8-18 now splits TURN_RETRIEVER_IDS from TOOL_RETRIEVER_IDS, and src/core/context-selection.ts:179 enforces it: `if (lineage && !TURN_RETRIEVER_IDS.has(lineage.retriever)) return undefined`. The type comment at :55 was updated from "never a turn selection" to "rejected by canonicalContextSelection" — the claim now matches the check.

### `docs/gap-audit/remediation/git-verbs.md`

Almost all resolved — this package was actually remediated. RESOLVED — #1 (restore of an untracked path silently deletes it): src/git/workspace-adapter.ts:886-900 now reads every content plane before writing and throws `path-not-tracked` when a path has neither stage nor head, with a comment naming Git's own "pathspec did not match any file known to git" behaviour. RESOLVED — #2 (restore advertised 4096 paths but validated 512, and was not chunked): restore is now routed through the chunker at src/git/client.ts:250. RESOLVED — #3 (chunkedPathMutation masked version-conflict as partial-mutation on a first-chunk failure): src/git/client.ts:294-300 now re-throws pre-write failures unwrapped — "a failed fence must stay 'version-conflict', a refused path 'path-not-tracked'". RESOLVED — #4 (features.clone/fetch/push hardcoded available:true): src/git/workspace-adapter.ts:1690-1692 now computes each via `remoteFeature(..., permittedOrigins)`. RESOLVED — #5 (vacuous assertion over an empty frozen array): src/git/validation.test.ts:52-56 now adds a probe entry so the comparison is proved live, with the comment "The constant is empty today, which makes the line above pass for free." RESOLVED — #6 (no pin that connect-src grants 'self'): src/git/validation.test.ts:59 `expect(sources).toContain("'self'")`. STILL OPEN — #7 (the "announced" seed-ignore claim is a bare console.warn): the overstated sentence was removed from docs/BROWSER_GIT.md, but the code comment at src/git/workspace-adapter.ts:1062-1065 still asserts "the omission is announced instead of being inferred from an empty status" while the only output remains `console.warn` at :1070 — nothing reaches the UI, the mutation result, or the approval record. n/a — #8 is a critique of a benchmark number in the package's own report, not a tree defect; #9 was verified clean by the reporter. UNVERIFIED — the report's not-done list (cherry-pick, conflicted status, UI surfaces, item #11 simulated-git removal); those were out of the package's scope and I did not check them.

### `docs/gap-audit/remediation/ui-polish.md`

Five of seven verify as resolved, one is mostly resolved, one I cannot check under the read-only constraint. RESOLVED — #2 (same-depth ordered/unordered list transition): src/ui/chat/markdown.tsx:89 now breaks the run with `if (depth === 0 && candidateOrdered !== ordered) break;`, restoring the two-block emission. RESOLVED — #3 (unguarded `*italic*` italicising ordinary prose): src/ui/chat/markdown.tsx:274 is now `\\*(?![\\s*])[^*\\n]*[^\\s*]\\*` with CommonMark flanking on both ends, and the rationale at :263-267 names the exact cases the verifier proved (`3 * 4 * 5`, `rm *.log *.tmp`). RESOLVED — #4 (Retry silently drops attachments while its title promises "the same prompt"): src/ui/chat/retry-prompt.ts:37 now fails closed (`if (previous.parts.some(part => part.kind === "attachment")) return undefined`) with the reasoning at :22-26, and src/ui/app.tsx:5533, :5575, :5591, :5637 now carry `originatingAttachments` through the resume and retry paths rather than dropping them. RESOLVED — #6 (tautological reassembly assertion): src/ui/chat/highlight.test.ts:94-96 records that the algebraically-vacuous check was replaced. RESOLVED — #7 (untested new behaviour): src/ui/approval-dock.test.ts now exists (5.5K), and src/ui/scroll-affordance.test.ts:86,105,165-170 drives a hand-written `stubResizeObserver` through the previously unexercised branch, with the note at :162 that "The runtime has none, so without" it the branch was dead. MOSTLY RESOLVED — #5 (focus-trap coverage overstated): the suite is now 13 tests and genuinely covers `focusStopIncluded` including the hidden, aria-hidden, collapsed-`details` and visible-summary cases (src/ui/focus-trap.test.ts:22-62); the residue is that `trapFocus()` (src/ui/focus-trap.ts:94) and `focusableWithin()` (:77) still have no direct test — src/ui/focus-trap.test.ts:2 imports only FOCUSABLE_LIMIT, FOCUSABLE_SELECTOR, focusStopIncluded and focusTrapTarget. UNVERIFIED — #1 (release gate failing at `gzip 136.93 KiB > 132.00 KiB`): scripts/release-gate.mjs:28 shows the 132 KiB cap is unmoved, and the branch brief states npm run check passes, which would mean the overage was absorbed; I could not confirm it because running the build is outside this task's read-only rule. A single `npm run check` (or `npm run build`) settles it.

### `docs/gap-audit/remediation/vault-storage.md`

none identified — I checked all seven code findings and each is fixed. #1 OVERCLAIM (the Drive default predicate had zero consumers): src/ui/platform-shell.tsx:463-469 `resolveDefaultVaultBackend` now ends `?? (isDeployableGoogleOAuthClientId(googleClientId) ? "google-drive" : "local-device")`, and docs/GOOGLE_DRIVE_VAULT.md:213-217 now says 'Both `GoogleIdentityServicesAuthorizer` and `resolveDefaultVaultBackend` consult it' — the doc sentence and the code now agree. #2 MISATTRIBUTED PROOF / latent literal-false type: src/vault/local-lab-live.ts:61 now types `deletionAvailableInRuntime: boolean` (was literal `false`) and :281 derives it from `cleanup.deletionAvailableInRuntime` instead of a hardcoded literal. #3 mergeIndex dropping other tabs' rows: src/storage/client-ciphertext-cache.ts:459-466 no longer has the `if (!local) continue` drop, and :328-330 evicts to budget over the merged view before writing. #4 unverifiable destructive sibling purge: src/storage/client-ciphertext-cache.ts:933 now takes each sibling's exclusive lock with `ifAvailable: true` and reclaims nothing otherwise — :921 states 'so nothing is reclaimed rather than guessing that a sibling is stale'. #5 validation loosened: src/storage/google-drive-auth.ts:115 now stores `clientId.trim()`, so the trimmed value is what reaches `client_id` at :177. #6 test coverage regression: src/storage/caching-object-store.test.ts:62-65 now supplies a healthy empty `list()` with a comment naming this exact trap, so the quota-throwing `write()` is reached again. #7 thin coverage on the new module: src/storage/workspace-key-handle-store.test.ts:122-183 adds `describe("origin-private key handle persistence")` with two tests driving `IndexedDbWorkspaceKeyHandleStore` and its read-back validator through a `FakeIndexedDb` (:203+). #10 remains structurally unverifiable here (full `npm test` and the MinIO live run) — barred by READ ONLY; settled by `npm run lab:start && npm run lab:test`.

### `docs/gap-audit/remediation/wasi-artifact-channel.md`

Three of the substantive issues RESOLVED, two structural notDone items STILL OPEN. Issue 3, the confirmed undisclosed regression (`install_execution_runtime` hardcoding the boot timer to 30 s and ignoring the caller's `timeoutMs`) — RESOLVED: src/tools/execution-tools.ts:170 and :405 now thread the caller's value through, src/tools/execution-tools.ts:833-840 carries the reasoning ('This tool does nothing but cold-start an interpreter, so the caller's timeoutMs has to bound the boot as well as the one-line probe that follows it') and passes `{ bootTimeoutMs: timeoutMs }` at :846, and the schema at :161 now says 'Bounds the whole activation, cold start included.' Issue 4, the model-facing gap where `execute_code` advertised a 10 s bound that excluded a 30 s interpreter boot — RESOLVED in the place the report said nothing the model reads had it: src/tools/execution-tools.ts:224 now describes 'Bounds the job's own statements only. A python-pyodide cold start is bounded separately (up to 30 s) and reported as bootMs, so total wall clock can exceed this.' Issue 6, the drifting duplicate 512-char ceiling — RESOLVED: src/tools/execution-tools.ts:21 now imports `WASI_PREVIEW1_MAX_WORKSPACE_ERROR_CHARS` from src/execution/wasi-preview1-contract.ts:8 instead of redefining it. STILL OPEN: (a) the shared workspace-egress module — src/execution/workspace-egress.ts still does not exist (`ls src/execution/` confirms), and the guard is still triplicated across src/execution/wasi-preview1-pack.ts:412, src/execution/wasix-pack.ts:418, and the separate `PYTHON_WORKSPACE_EXCLUDED_SEGMENTS` set at src/tools/execution-tools.ts:38 used at :1271 and :1310 — and the surface has since grown a fourth 512-char twin at src/execution/shell/contract.ts:71 (`AIRSHIP_SH_MAX_WORKSPACE_ERROR_CHARS = 512`), so the drift the report warned about has one more place to happen. (b) Issue 5, the internal inconsistency where the Python egress guard discards a completed run's exit code and streams while the collection-overflow path keeps them — the throwing guard is unchanged at src/tools/execution-tools.ts:1310-1311, so the two contracts still differ; the report judged this defensible, and I found nothing reconciling them.

### `docs/gap-audit/shell.md`

FIVE VERIFIED STILL OPEN, five verified resolved.

STILL OPEN — #3 "WASI stdin is hardcoded to an empty file and ExecutionRequest has no stdin field". Both halves hold. src/execution/wasi-preview1-worker.ts:64 still constructs the fd table with `new OpenFile(new File([]))` as fd 0, and the ExecutionRequest type at src/execution/runtime-registry.ts:98-111 has no stdin member (args, env, command, sourcePath, workspaceRoot, workspace, writeBack, timeoutMs, signal, onOutput). The only `stdin` reference anywhere in src/execution or src/tools is src/tools/execution-tools.ts:1644, `pyodide.setStdin({ stdin: () => null })` — which closes Python's stdin rather than supplying one.

STILL OPEN — #7 "The terminal shell cannot call Airship's own Git, Python, or WASI runtimes", but now DECLINED WITH A STATED REASON rather than silently unresolved. src/execution/shell/utilities.ts:12-17: "`git`, `python`, and a WASI command are deliberately NOT here. Airship owns real engines for all three, but each lives behind an approval-gated tool with its own workspace transaction; routing them through this table without that gating would create a bypass, so the honest answer for now is that the shell does not provide them." That is a good decline under CANON.md:157-163 (capabilities fail closed; approval policy decides), but the capability gap the finding describes is still present.

STILL OPEN — #8 "WASI/Python jobs are capped at 10 s while WebContainer gets 120 s". The asymmetry is unchanged: src/tools/execution-tools.ts:224 caps execute_code at `maximum: 10_000` while src/tools/execution-tools.ts:318 caps execute_node_project at `maximum: 120_000`. The output half is also unchanged — MAX_OUTPUT_CHARS = 256 * 1_024 at src/execution/wasi-preview1-pack.ts:21 and src/tools/execution-tools.ts:33.

STILL OPEN — #9 "One preopen named '.' at the workspace root, no cwd control and no scratch directory". src/execution/wasi-preview1-worker.ts:59 is still the single `const preopen = new PreopenDirectory(".", root.contents)`, and grep for cwd/scratch/preopen across src/execution/wasi-preview1-pack.ts returns nothing. The one improvement is adjacent, not to this finding: src/execution/wasi-preview1-worker.ts:28-32 now distinguishes a mounted run from an unmounted one so "an unmounted run must never be failed by a scratch file the guest wrote into its throwaway preopen."

STILL OPEN — #10 "The WASI shim implements clock waits as a spin loop, so any guest sleep burns the worker at 100%". Verified in the installed dependency: node_modules/@bjorn3/browser_wasi_shim/dist/wasi.js, poll_oneoff, `while(endTime>getNow()){}`. package.json:50 still pins "@bjorn3/browser_wasi_shim": "0.4.2" — the exact version the audit measured. This one is not locally fixable without patching or replacing the dependency, which is what the finding says.

RESOLVED (verified) — #1 "no usable artifact channel: base64-in-a-tool-argument only": wasmPath landed as the audit's fix (1) recommended, at src/tools/execution-tools.ts:218 (schema), :236 (normalizeWorkspacePath), :240 (fail-closed without a bound workspace, "A wasmPath command artifact has no bound Airship workspace"), :251 (workspace binding even with no mounted root), and mirrored in the proxy schema at src/tools/execution-tool-proxies.ts:89. Fix (3) also landed: src/execution/wasi-preview1-worker.ts:22-23 now carries `wasm: Uint8Array` with the comment "Structured clone carries the artifact bytes; base64 never crosses here."

RESOLVED (verified) — #2 "No POSIX shell exists in any browser tier": the audit's recommended universal tier was built. src/execution/shell/ now holds a complete TypeScript interpreter — lexer.ts, parser.ts, interpreter.ts (29.1K), expansion.ts, arithmetic.ts, pattern.ts, streams.ts, builtins.ts (21.3K), utilities-files.ts, utilities-text.ts (29.5K), test-expression.ts, script-suite.test.ts (21.2K). It is a first-class runtime in the registry: src/execution/runtime-registry.ts:70-71 adds the `posix-sh-script` command interface and :77-78 the `airship-sh` shell value with the comment "`airship-sh` is a POSIX-sh-compatible interpreter, never GNU Bash", plus an `in-page-interpreter` isolation class (:83) and an `abort-interpreter` cancellation class (:96-99) named rather than borrowed so no reader is told a thread was killed when none exists.

RESOLVED (verified) — #4 "Python job timeout starts before CPython boots": src/execution/runtime-registry.ts:119-122 adds `bootMs`, "reported separately because it is not charged against the job's own timeoutMs budget", and the schema now says so to the model at src/tools/execution-tools.ts:224 — "Bounds the job's own statements only. A python-pyodide cold start is bounded separately (up to 30 s) and reported as bootMs."

RESOLVED (verified) — #5 "Python writeback can adopt files into .git/.airship/node_modules": src/execution/runtime-registry.ts:143-150 adds `refusedPaths` — "Paths the job produced that the egress guard refused to report or adopt (control-plane, `.git`, `.airship`, `node_modules`)" — with a bounded `refusalReason` at :152, and states the correctness rule the audit asked for: "The run keeps its exit code and streams — a refused write must not destroy a completed run — but the refusal is named so nothing vanishes silently."

RESOLVED (verified) — #6 "WASI and Pyodide throw away a successful run's exit code and output when workspace collection exceeds budget": src/execution/wasi-preview1-worker.ts:94-99 now says "A run that completed is reported with its own exit code and streams even when its generated files exceed the mount budget", collects inside a try/catch into `workspaceError` rather than failing the run, and deliberately leaves `files` ABSENT rather than empty because "an empty list is indistinguishable from 'the guest deleted everything' and would make the reconciler report every mounted path as deleted." The surfaced contract is at src/execution/runtime-registry.ts:132-141.

### `docs/gap-audit/tool.md`

Most of this document is still open — it is the least-remediated set here. STILL OPEN — #1 (no jsh shell tool; execute_node_project's description denies the shell exists): grep for `execute_shell_command` across src/tools/ returns nothing. STILL OPEN — #2 (search_text scans only the alphabetically first 512 files, no cursor): src/tools/workspace-tools.ts:14 `MAX_SEARCH_FILES = 512`, :223 `allEntries.slice(0, MAX_SEARCH_FILES)`, :256 sets `truncated` — and the schema still has no offset or cursor parameter. STILL OPEN — #3 (no sub-agent primitive): no `run_subagent` anywhere under src/tools/. PARTIALLY OPEN — #4: the history half is fixed (git_inspect gained log/show/tags/stash at src/tools/git-tools.ts:18, and git_change gained merge/stash/restore/reset at :76), but push and worktree create/remove are still withheld — git_remote's action enum is still `["clone", "fetch"]` at :197 and no `git_push` tool exists. STILL OPEN — #5 (read_file has no offset/limit and hard-fails past the 1 MiB cap): src/tools/workspace-tools.ts:95-105 still declares `properties: { path: { type: "string" } }` only and calls `workspace.read(path)` whole, never the existing `readBounded`. STILL OPEN — #6 (no negative capability declaration in the prompt manifest): src/core/operating-charter.ts:77 still composes only `installedToolSection` (:134); there is no capability-boundary section. STILL OPEN — #7 (no web_search): no such tool under src/tools/, and the specific copy defect the finding flagged is verbatim intact — src/tools/network-tools.ts:51 still blames the remote ("The site may be offline or may not grant Airship CORS access") for what can equally be Airship's own CSP refusal.

### `docs/gap-audit/vault.md`

Three resolved, five still open — and one of the five is open in a way the document could not have predicted. STILL OPEN, and the most consequential — #2 `drive-vault-forgets-key-and-folder-on-reload`: the recommended module was built exactly as specified and then never wired. src/storage/workspace-key-handle-store.ts exists with its own `airship-workspace-key-handles-v1` database (:9), the `google-drive:<googleSubject>` partition keying the audit insisted on (:59), live-rediscovery adoption (:91), and a full test file (src/storage/workspace-key-handle-store.test.ts, 13.4K) — but `googleDriveKeyPartition`, `adoptCachedWorkspaceKey`, `rememberWorkspaceKey`, `openWorkspaceKeyHandleStore` and `MemoryWorkspaceKeyHandleStore` have zero call sites anywhere in src/ or e2e/ outside that test. The only external consumer of the module is src/storage/local-device-keyring.ts:9, importing `equivalentWorkspaceKeys`. So a Drive vault still requires the recovery-key paste on every reload. docs/GOOGLE_DRIVE_VAULT.md:65-70 states this honestly ("Not yet reachable from the product"), so the tree is honest — but the finding is not closed. STILL OPEN — #3 `drive-full-index-round-trip-per-write`: every mutating path still forces a full index refresh and full re-upload. src/storage/google-drive-object-store.ts:155 (putIfAbsent), :177 and :187 (compareAndSwap), :221 (trash) all call `loadIndex(signal, true)` with forceRefresh, then `advanceIndex(loaded, next)` writes the whole index back. The read cache added at :283-307 helps repeat reads only. STILL OPEN — #5 `no-unlock-ceremony-passkey-prf-absent`: zero `navigator.credentials` references anywhere in src/. This one is explicitly sanctioned by CANON.md:1114 ("passkey-PRF unlock, enrolled-device key wrapping/revocation... " under Planned), so it is an open finding, not a contradiction. STILL OPEN — #7 `workspace-reads-never-use-ranges`: `getRange` has exactly one production consumer, src/storage/encrypted-segments.ts:138, and encrypted-segments is imported only by src/retrieval/context-driver.ts:3, src/retrieval/publisher.ts:3 and src/retrieval/contracts.ts:2 — the context-fabric path, precisely as the finding said. Workspace and Git reads still go through src/vault/encrypted-workspace.ts, which uses whole-object `store.get`. STILL OPEN — #8 `local-device-range-reads-whole-record`: src/storage/local-device-object-store.ts:244 calls `this.get(key, signal)` (full fetch and decrypt) and then slices at :251. The capability descriptor is honest about this — it declares `providerEvidence: "in-process"` at :134-138 — so it is an efficiency gap, not a false claim. RESOLVED — #1 (src/ui/platform-shell.tsx:463-469 deployment-aware `resolveDefaultVaultBackend`, the second hardcoded default closed at :625-635 where `loadPreferenceOverrides` revalidates a stored `google-drive` against build availability, and .github/workflows/pages.yml:147,151 ships the variable plus a matching default); #4 (src/storage/google-drive-object-store.ts:212 `trash()` with index-entry-first reclamation, the linearization-point reasoning at :204-211, and confirmed-absent gated on Drive's own `trashed: true` echo at :280); #6 (src/storage/client-ciphertext-cache.ts:292-305 `evictToBudget` with entry and byte ceilings, invoked at :330 and :374, and the whole-origin-quota rationale at :158-163).

### `docs/gap-audit/wave2/browser-extension.md`

none identified — the two CONFIRMED defects and the lesser gaps are all fixed, several with a source comment naming the original defect. #1/#2 TOCTOU (concurrency cap and duplicate-request-id checked before `await resolveCapabilities()`): extension/src/relay.ts:344-351 now carries the comment 'Admission and reservation are one synchronous step, deliberately ... A check whose result is acted on *after* an await is not a check', with `inflight.has(request.id)` at :352, the cap check at :359 and `inflight.set` at :377 and no await between them. #3 encoded-separator gaps (`..;`, `%252e%252e`) and #4 over-refusal of `%2F` in a query: both closed by inverting the filter — extension/src/policy.ts:224 `const DESTINATION_PATH = /^[A-Za-z0-9\-._~/]*$/u` draws the boundary positively, and :220-222 records 'The query is not covered ... an OAuth `redirect_uri` carries `%2F` by construction'. #5 top-frame check skipped when frameId is not a number: extension/src/policy.ts:438-441 now refuses on `sender.frameId !== 0`, with :425 stating 'A sender with no `frameId` is refused rather than accepted.' #4 (README overstating the caller path as a boundary): extension/README.md:28 now says '**The origin is the real boundary**' and :49-52 spells out that `https://sirouk.github.io/other-project/` is same-origin. #7 unbounded header-object walk: extension/src/protocol.ts:207 refuses at `limits.maxHeaderEntries` during the walk. #8 root tsc not covering extension/: package.json:47 now runs `typecheck:extension` inside `npm run check`. #9 no icons/packaging: extension/icons/ holds icon-16/32/48/128.png, and extension/package.mjs + package.test.mjs + extension/store/LISTING.md exist. #6 (Chromium modifyHeaders read-back vs Firefox addListener-did-not-throw both reported as 'live', and the README not distinguishing them) is UNVERIFIED — `grep -n '"live"|userAgentOverride' extension/src/user-agent.ts` returned no hits, so the vocabulary has changed and I could not confirm the distinction is now drawn; settled by reading extension/src/user-agent.ts in full alongside the README's user-agent row.

### `docs/gap-audit/wave2/connect-inference-ux.md`

Essentially all RESOLVED — this is the document the tree moved furthest past, including the overclaim that set honest=False. **Issue 1, the basis for honest=False** (`probeExtensionBridge` having zero production callers, the surface rendering a compiled-in `NO_BRIDGE_CLIENT`) — RESOLVED: src/ui/access-view.tsx:35 imports it, src/ui/access-view.tsx:220 wires it as the default (`observeExtensionBridge = probeExtensionBridge`), src/ui/app.tsx:2082-2083 dynamically imports and calls it, src/ui/connect/connect-lanes.ts:114-118 takes `bridge: ExtensionBridgeObservation | undefined` with the comment '`undefined` while that observation is still in flight — never a stand-in for "no extension", which only a settled observation may say', and `NO_BRIDGE_CLIENT` no longer appears in src/. src/ui/access-view.copy.test.ts:122 pins the wiring. Issue 2 ('Add the Airship extension' offered where it cannot work) — RESOLVED: src/ui/connect/connect-lanes.ts:533-535 emits that label only when `host.kind === "installable" && input.extensionInstallUrl`, with :121-123 stating the rule ('an instruction with nowhere to go cannot be followed'), and the fallbacks at :537-541 say 'Extension not published here' / 'Not possible in this browser' instead. Issue 3 (Chutes lane summary re-offering sign-in one line above 'sign-in isn't available') — RESOLVED: the summary is now the credential-free `CHUTES_LANE_QUALIFIER = "Encrypted inference with per-turn evidence"` (src/ui/connect/connect-lanes.ts:144) with no 'Sign in, or paste an API key' clause. Issue 4 ('Check this machine' checking nothing) — RESOLVED: src/ui/connect/connect-surface.tsx:58 and :637 document a real loopback request, :688 renders a 'Checking 127.0.0.1…' in-flight state, and e2e/connect-inference.spec.ts:355 is a behavioural gate ('Check this machine issues a real loopback probe and keeps every other lane'). Issue 5 (the B3 fix covered only by a source-string grep) — RESOLVED: e2e/connect-inference.spec.ts:338 now covers it behaviourally ('the Chutes key panel is already open when this build has no sign-in'). Issue 6 (the whole surface vanishing once Chutes connects) — RESOLVED: src/ui/access-view.tsx:1040-1044 carries the fix and its reasoning ('...which left a connected person with no way to add a second provider — and made the connected lane state unreachable. Chutes state now decides only what sits inside the Chutes lane'), and ConnectSurface at :1075 renders unconditionally. Issue 7 (Grok lane test-locked into offering no API-key alternative) — RESOLVED: src/ui/connect/connect-lanes.ts:189 builds the lane with provider `xai` and :484 attaches the alternative 'An xAI API key connects straight from this page and needs no extension', now carried on every non-ready branch. Issue 8 (`BridgeHandshakeOutcome` documented as mirroring a type nothing bound it to) — RESOLVED: src/ui/connect/extension-bridge-presence.test.ts:3 imports the real `BridgeHandshakeResult` from src/inference/bridge/protocol.ts:245, and :18 names the defect ('Restating the record here is what let the...'). Issue 9 (extension-absent assertions proving only a constant) — RESOLVED as a consequence of issue 1: the lanes now render a settled observation, and e2e/connect-inference.spec.ts:243 gates it ('Claude and Grok state the extension honestly and offer no broken button'). Issue 11 (h1 'Connect models' vs h2 'Connect a model') — RESOLVED: src/ui/access-view.tsx:77-84 records the fix and demotes the phrase to `CONNECT_ROUTE_EYEBROW`. Only issue 10 remains UNVERIFIED, and only because confirming it requires running the release gate.

### `docs/gap-audit/wave2/page-side-bridge.md`

NONE IDENTIFIED — all seven code-level issues verified resolved in the current tree; the eighth is documentary.

(1) "CONCURRENCY CEILING NOT ENFORCED under simultaneous entry ... check-then-increment straddles an await" — RESOLVED. The ordering is inverted: src/inference/bridge/client.ts:200 performs the `>= maxConcurrentRequests` check and src/inference/bridge/client.ts:206 does `this.#activeFetches += 1` BEFORE the `await this.#observePresence()` at :209, so no await separates the check from the increment. Both release paths are guarded — :233 `if (!handedOff) this.#activeFetches = Math.max(0, this.#activeFetches - 1)` and :252.

(2) "PAGE ALLOWLIST MATCHES RAW STRING PREFIXES, NOT A NORMALIZED URL" — RESOLVED, including the false comment. isBridgeDestination at src/inference/bridge/protocol.ts:348-361 now parses (`new URL(url)` at :353) before comparing, and adds https-only (:357), embedded-credential and fragment rejection (:360, "Embedded credentials would be sent to the host, and a fragment is never transmitted"), and a path regex (:361). The comment the report called "not literally true" was rewritten to state the reason at src/inference/bridge/protocol.ts:30-33: "A raw prefix test would accept https://api.anthropic.com/v1/../../evil, which the extension then refuses; that would make the extension's check load-bearing instead of redundant, and the sentence above would be false."

(3) "NO FIRST-BYTE/HEAD DEADLINE for a bridged exchange" — RESOLVED. A second, independent timer exists: src/inference/bridge/client.ts:281-289 arms `headTimer` for `this.#limits.headTimeoutMs` with the message "The extension bridge accepted the request but sent no response head before the first-byte deadline", cleared when the head arrives at :320. The limit is declared and justified at src/inference/bridge/protocol.ts:90-93 ("Separate from requestTimeoutMs because a long *answer* is normal") and set to 60_000 at :117, against requestTimeoutMs 300_000 at :118, with the rationale at :116 — "the whole requestTimeoutMs on an extension that answered nothing."

(4) "PRESENCE IS MEMOIZED FOR REQUEST GATING" — RESOLVED as a disclosure defect. The memo still exists by design (src/inference/bridge/client.ts:445-451, presenceTtlMs 30_000 at src/inference/bridge/protocol.ts:126), but the doc comment at src/inference/bridge/client.ts:442-444 now names the exact consequence the report said the summary understated: it can "refuse for up to presenceTtlMs on an observation no longer being made."

(5) "TEST-COUNT ATTRIBUTION IS LOOSE" — n/a to the tree; a correction to a superseded report.

(6) "CROSS-PACKAGE DUPLICATION ... NO unavailable[] arm, so the extension's own named per-provider reason cannot reach the connect surface" — RESOLVED. src/ui/connect/extension-bridge-presence.ts:94 now reads exactly that arm: `return observation?.unavailable.find((entry) => entry.provider === provider)?.reason`. The structural restatement was replaced by real type-only imports at :21-22 with the reason recorded at :25 ("Both imports are type-only and erase at build time").

(7) "oauth-transport.ts hardcodes 8 * 1_024 instead of importing MAX_OAUTH_REQUEST_BODY_BYTES" — RESOLVED. src/inference/bridge/oauth-transport.ts:15 imports the constant and :111 uses it: `request.body.length > MAX_OAUTH_REQUEST_BODY_BYTES`.

(8) "bridgeProviderOfUrl applies only the prefix test, skipping maxUrlChars and control-character checks" — RESOLVED. src/inference/bridge/oauth-transport.ts:224-229 now delegates to the strict function: `for (const provider of BRIDGE_PROVIDER_IDS) { if (isBridgeDestination(provider, url)) return provider; }`, which carries the maxUrlChars check at src/inference/bridge/protocol.ts:349 and the control-character check at :350.

### `docs/gap-audit/wave2/provider-oauth-core.md`

RESOLVED — issue #1 (dead console.anthropic.com fallback) and #2 (a green test proving an impossible path): src/auth/provider-oauth/registrations.ts:134-140 now carries a single endpoint with a comment naming both defects — "the fallback could never run in production, and the test that 'proved' it only did so against a stub"; src/inference/bridge/protocol.ts:35-39 confirms the allowlist still has no such prefix. RESOLVED — issue #4 (pkce.ts comment drift): src/auth/provider-oauth/pkce.ts:11-13 now reads "48 random bytes → a 64-character verifier" with PKCE_STATE_BYTES documented separately. RESOLVED — security finding #1 (direct transport gated by provider, not destination): src/auth/provider-oauth/transport.ts:116-139 now documents and enforces "The direct transport's compiled-in destination allowlist... A URL is an allowlisted direct destination only for the provider that owns it." STILL OPEN — security finding #3 (pasted redirect URL origin is unchecked): src/auth/provider-oauth/authorization-code.ts:129-148 still parses any `https?://` paste and extracts the code from its query or fragment without ever comparing `url.origin` to `registration.redirectUri`; the equality check at :262 fires later on the attempt record, and the mitigations the reporter named (S256 binding, constant-time state check, honest `stateVerified: false`) are still what carries it. STILL OPEN and now worse than reported — issue #3 (doc/code contradiction never flagged): src/inference/providers/official-providers.ts:22-25 ships OpenAI with `state: "configured-public-pkce"` and the detail "Airship ships OpenAI's own Codex client with product-owner approval", while docs/PROVIDER_FABRIC.md:57 still says "No published third-party public-client grant" and docs/EXTENSION_BRIDGE.md:39 still says "No reviewed third-party Airship OAuth grant is published; UI remains unavailable". docs/INFERENCE_PROVIDER_REGISTRY.md:20 was softened to acknowledge a research descriptor but still asserts "no production controller is enabled". The approval claim is user-visible: src/ui/provider-fabric-panel.tsx:239 renders `provider.descriptor.oauth.detail` verbatim. The mitigating fact is that no OAuth control is offered — provider-fabric-panel.tsx:228 resolves only an `api-key` method for cloud providers — so the UI half of the docs' claim still holds. STILL OPEN (minor) — issue #7: the review record at src/inference/providers/official-providers.ts:62-66 (`openai-codex-live-cors-2026-07`, reviewedAt 2026-07-25) is still asserted metadata that nothing verifies at runtime.

