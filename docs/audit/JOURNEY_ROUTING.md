# Journey routing — every finding has exactly one owner

Generated from `.ui-capture/atlas.json`. 152 findings, 152 routed, 0 unrouted.
Assertion: `scripts/journey-atlas-gate.mjs` checks that the per-journey lists sum to this
total, that every finding has a unique id and a lane, that routing is a permutation of the
Atlas in both directions, and that every count stated here is a count this file contains.
It is wired into `npm run check`; before it existed this line was a claim with nothing
under it, and four findings sat in the Atlas prose owned by no lane at all.

## L1-chat-composer — 27 findings

| id | severity | link | persona | finding | evidence |
|---|---|---|---|---|---|
| J021 | misleading-state | response | power-user — wee | A branch opens looking like a brand-new empty conversation while carrying hidden ancestor context into the model | Edit & branch on turn 3: notice "Carrying 4 ancestor messages; none omitted." and header "Continued from the source conversation at event 17 · 4 ancestor messages carried, 0 omitted." — with `document.querySelectorAll('.message').length === 0` and the newcomer empty state on screen: "Open a terminal · Real processes in this tab, no accoun |
| J022 | trust-error | proof | power-user — wee | The carried context is unreadable anywhere, including the audit surface that should hold it | The same branch's row in All conversations reads "RUNTIME RECORD Manifest pins and transcript · 0 messages", and after sending one turn it reads "· 2 messages" — never 4 or 6. The only trace of the inherited messages is the count sentence and "Event 2 · session.fork.context.seeded · sha256:VUd6Uqu7…". |
| J023 | breaks-continuity | continuation | power-user — wee | Branches name themselves by concatenation, so different branches of one conversation become indistinguishable rows | Two Edit & branch operations on different turns of the same conversation both produced the title "Q1 about retrieval · edit"; two retries produced "Base question · retry" then "Base question · retry · retry", and a further edit produced "Base question · retry · retry · edit". In the list column the title element renders 119px of 353px of  |
| J025 | friction | continuation | power-user — wee | Every branch is a top-level conversation, so one line of thought inflates the library | After one base question, two retries and one edit the list header read "4 conversations" — rows "Base question · retry · retry · edit", "Base question · retry · retry", "Base question · retry", "Base question" — all created inside two minutes from a single prompt. |
| J029 | inaccessible | completion | keyboard-a11y —  | The completion announcement is supposed to carry up to 200 characters of the reply and carries none of it on the default path every cold visitor takes. | src/ui/chat/streaming-slot.tsx:66-79 arrivalAnnouncement() returns `Airship’s turn ended. ${prose}` and only falls back to the bare sentence `if (!prose)`. Observed value of that region, rAF-sampled: `t=1597ms [sr-only#1 atomic=true] "Airship’s turn ended."` — nothing else. The reply on screen at that instant: 'Airship is running this tur |
| J030 | misleading-state | response | keyboard-a11y —  | Two polite live regions change in the same frame at turn end, so the kernel sentence and the completion sentence race — the precise failure the code was written to eliminate. | rAF trace: `t=1597ms [sr-only#0] "Local kernel ready"` and `t=1597ms [sr-only#1 atomic=true] "Airship’s turn ended."` (phone: both at t=1566ms). The comment at src/ui/chat/streaming-slot.tsx:126-130 states the goal verbatim: "before this region existed, nothing announced that the answer had landed — the closest thing was the topbar runtim |
| J031 | misleading-state | in-flight | keyboard-a11y —  | The only in-flight utterance names a storage operation, not the model working, and is followed by 1.5 seconds of silence with no announced way to stop. | The complete in-flight channel for a turn is one string: 'Persisting turn intent' at t=68ms, then nothing until t=1597ms. Nothing says Airship is answering, nothing reports progress, and no cancel/stop control ever appeared in the focus chain during the turn (focus sampled at 150/300/600/1200/2500/4000/6000ms was `textarea "Message Airshi |
| J033 | inaccessible | completion | keyboard-a11y —  | Local-command successes are equally silent, so the whole lane is mute in both directions — there is no utterance that distinguishes a completed command from a rejected one. | `cmd=/help []` — zero announcements while a full command listing ('/deactivate-execution-runtime <runtime> [--json <json>] — Terminate an optional runtime a…') was written into the transcript. Contrast the same composer's prose path in the same session, which did announce: `cmd=tell me something [{"v":"Persisting turn intent"},{"v":"Local |
| J034 | misleading-state | proof | keyboard-a11y —  | A failed turn is styled as an error on screen but carries no error semantics in the accessibility tree, so even browse-mode reading gives no signal that this message is a failure. | `role=alert nodes on the page: []` while two messages carry the visible badges 'FAILED TURN' and 'EXCLUDED FROM PROVIDER CONTEXT'. The messages are plain <article aria-label="Airship message"> — the same accessible name as a successful reply — with liveAncestors: []. |
| J051 | breaks-continuity | in-flight | Researcher holdi | A source pulled into the thread is explicitly excluded from the model's context, so evidence gathered in chat cannot be reasoned over. | `/read notes/retrieval.md` returns the file body under "Local result · excluded from model context" with badges "COMPLETED TURN" and "EXCLUDED FROM PROVIDER CONTEXT" on both the request and the reply. No affordance converts a read source into turn context. |
| J075 | recovery-failure | recovery | mobile — someone | The retry the copy instructs you to perform cannot succeed, and the control gives no sign of that | "Provider unreachable. Check connectivity and retry; local state was kept." with "retry control: {\"present\":true,\"disabled\":false}" and field value "" — network restored, retry tapped, same banner |
| J083 | breaks-continuity | continuation | mobile — someone | The resumed conversation's column is 610px wide inside a 390px phone and the clipped 220px cannot be panned to | "AT REST: {cw:390, mainScrollW:610, mainOverflowX:'hidden', nearestHScroller:null, scrollX:0, mainScrollLeft:0}" and after two full-width CDP touch pans the same numbers. The user's own message measures right=549 (159px cut); the assistant reply renders as "Airship is running this turn entirely on your dev…" |
| J094 | lost-work | proof | Returning — some | Local-command turns vanish from the resumed transcript, so a returning person cannot tell what the agent did versus what it said. | Three user turns were sent (incl. "/write pricing-notes.md Competitor A 49..."). Resumed transcript contains "Draft the Q3 pricing memo intro paragraph." and "Now list three competitor price points I should check." but no /write row and no "FAILED TURN" badge; #sessions reports "RUNTIME RECORD  Manifest pins and transcript · 4 messages". |
| J102 | trust-error | response | failure-recovery | The failure footer claims a partial answer was kept when the stream produced nothing. | Screenshot .ui-capture/journeys/fr-j2-02-after-short.png: "TURN FAILED — PARTIAL RESPONSE KEPT." with an empty region beneath it. Source: src/ui/chat/turn-recovery.ts:63 pushes the text part only `if (partial)`, but line 81 `next.push(footer)` appends the "— partial response kept." summary unconditionally. Reproduced on three separate fai |
| J103 | misleading-state | response | failure-recovery | The provider's own diagnostic arrives on the wire and is discarded, replaced by a framing complaint that sends the person to debug the wrong thing. | LM Studio returns HTTP 200 then `event: error` / `data: {"error":{"message":"Engine protocol predict request returned 400: ... Failed to initialize samplers: failed to parse grammar"}}`. Airship shows "The local inference stream ended before a completion marker." `consumeSse` in src/inference/local/openai-transport.ts only reads `message. |
| J104 | trust-error | proof | failure-recovery | "invocation checked" is earned by a probe that omits the tool manifest every real turn carries, so Connection certifies a connection that cannot serve a single turn. | Captured bodies: probe /tmp/airship-req-1.json = 230 bytes, no `tools`; title /tmp/airship-req-2.json = 357 bytes, no `tools`; real turn /tmp/airship-req-3.json = 47 KB with `tools` length 32. Probe and title succeed; the turn fails 100% of attempts. On screen throughout: "LM Studio session ready · invocation checked" and provider card "R |
| J105 | friction | continuation | failure-recovery | A failed turn does not return the person's prompt to the composer, although a user-initiated Stop does — so the product treats its own failure as less worth recovering than a deliberate cancel. | After Stop: composer value "stream me a long list please" (restored, and it correctly does NOT clobber a newer draft — verified with a 137-char second draft that survived). After a failure of the identical turn: composer value "". |
| J108 | lost-work | in-flight | failure-recovery | The inference connection dies on reload with no notice, and the next message is consumed by the deterministic demo and recorded as a completed turn with a receipt. | After the reload the chip reads "Demo · local" and the composer credential chip reads "Local demo"; sending "What is my project codename?" produced "Airship is running this turn entirely on your device with the deterministic demo provider. Try /write notes/hello.md…" plus "TURN COMPLETED.", "Final response · local receipt 94880311" and cl |
| J111 | trust-error | in-flight | failure-recovery | Every local-model turn is hard-capped at 30 seconds of total wall clock and the expiry is reported to the person as their own cancellation. | Measured: unattended turn failed at t+32.07 s with 93 streamed lines on screen, message "Turn failed / The local model request was cancelled." Source chain: src/inference/local/http.ts:150 `timeoutMs: boundedInteger(options.timeoutMs, 30_000, 1_000, 300_000)` → src/inference/local/lm-studio.ts:87 `totalTimeoutMs: this.http.timeoutMs` → ap |
| J113 | friction | recovery | failure-recovery | On a phone the recovery actions for a failed turn are a second-level disclosure behind an unlabelled ellipsis, and two of the three are disabled. | 390×844 DOM after a cut stream: summary text "•••" (44×44), and inside it {"n":"Copy","disabled":false,"w":208,"h":44}, {"n":"Retry","disabled":true,"w":208,"h":44}, {"n":"Fork from here","disabled":true,"w":208,"h":44} — while the card above reads "Retry is available." |
| J118 | friction | persistence | long-session — s | Per-turn retention is unbounded even in the healthy case, and one build in this session retained 40x more with nothing to catch it. | Current build, 120 turns: heap after forced GC rose 13.0 → 24.5 MB in exact 1.28 MB steps every 10 turns, never plateauing; retained nodes 1,781 → 1,961 (+2/turn) with live DOM flat at 937. Two earlier sessions (bundle index-CvxcVy7i.js, since replaced by index-Cmq-JyfS.js at 14:45:58) grew +5.57 MB, +149 nodes and +17 JSEventListeners pe |
| J121 | friction | in-flight | long-session — s | The backlog silently halves the reading area of the conversation it belongs to. | Transcript scroller measured 483 px of 900 px (54%) at 1440x900 and 348 px of 844 px (41%) at 390x844 while 21 items were queued, versus 566 px of 844 px (67%) on the same phone with an empty queue. |
| J125 | inaccessible | continuation | long-session — s | Screen-reader users returning to a busy terminal are told to do the work manually. | Live-region text on return from chat, verbatim: "Too much output to announce, navigate to rows manually to read". |
| J126 | friction | response | long-session — s | Main-thread blocking per turn grows with conversation length on a phone, so the composer gets less responsive as the day goes on even though turn latency looks flat. | PerformanceObserver longtask totals at 4x CPU throttle, 390x844: turns 1-20 = 32 tasks / 3,448 ms (172 ms per turn); turns 21-40 = 40 tasks / 5,927 ms (296 ms per turn); max single task 206 ms. Turn latency p50 was 1,647 ms throughout, hiding it. |
| J137 | trust-error | response | COMPLETENESS CRI | A runtime activation failure is surfaced as a raw build-asset URL inside the chat transcript, with no diagnosis and no route back to the page that offered the action. | Transcript: "Failed to fetch dynamically imported module: http://localhost:4173/assets/node-webcontainer-pack-DntNdFa_.js" / "FAILED TURN" / "EXCLUDED FROM PROVIDER CONTEXT". |
| J141 | friction | recovery | COMPLETENESS CRI | The only action that actually works — reloading the page — is named nowhere on the failure card. | The card's full content is the sentence plus one button, "Retry loading Memory". src/ui/route-failure.tsx is the shared component behind all ten spellings. |
| J149 | friction | continuation | long-session — d | The transcript mounts every turn for the life of the conversation, so cost grows without bound with session length | 1,542 → 3,468 DOM nodes and 237 → 471 listeners between turn 6 and turn 24; ~54 nodes and ~9.7 listeners per turn, no windowing observed |

## L2-continuation — 38 findings

| id | severity | link | persona | finding | evidence |
|---|---|---|---|---|---|
| J001 | misleading-state | entry | Novice — heard " | On a cold first open with nothing configured and no reload, the only warning-coloured sentence on the page claims the user already lost a conversation. The app writes `#chat/<id>` itself, replaces the session, then treats its own stale hash as an unresolvable conversation and fires the loss notice. | Verbatim, at y=880 on the first paint: "That conversation existed only in page memory and did not survive the reload. This is a new conversation." First seen 249 ms (desktop) / 234 ms (no-namespace) / 911 ms (mobile); still present at 6 s in all three. Navigation trace: `NAV /` → `NAV /#chat` → `NAV /#chat/b5d597f8-8b9b-43e8-9ce3-b0d011f5 |
| J009 | lost-work | persistence | Novice — heard " | A plain browser refresh destroys the entire conversation, and the recents list replaces it in the same slot with an empty conversation bearing the same timestamp, so the loss is invisible. | Rail before reload: "● My dog is named Biscuit and my flight is Tuesday. / Airship: Airship is running this turn entirely on your device with the … / 1:45 PM". Rail after reload: "● General conversation / No messages yet / 1:45 PM". The header count reads "General conversations 1" both before and after. Conversation id changed `21feb15b-f |
| J010 | trust-error | proof | Novice — heard " | The audit surface that exists to prove what happened reports a clean, resumable shelf immediately after four messages and two receipts were destroyed. | "All conversations" verbatim after the reload: "1 conversation … General conversation just now Active 2 events general airship/demo-v1 … Structure passed Ready to resume 0 receipts … RUNTIME RECORD Manifest pins and transcript · 0 messages". Nothing records that conversation `21feb15b…` ever existed or was discarded. |
| J014 | friction | action | power-user — wee | The palette is navigation-only — it holds no verbs, so every action still requires menu archaeology | Palette query "new conversation" → "No matching destination or command."; "switch" → "No matching destination or command."; "retry" → "No matching destination or command."; "rename" → only "/move-file". Meanwhile the live shell exposes buttons titled "New conversation", "Rename conversation", "Fork", "Retry" and "Edit & branch". |
| J015 | friction | entry | power-user — wee | With an empty query the palette lists zero conversations, so ⌘K ↵ cannot return you to what you were doing | Placeholder reads "Go to a view, session, or command…", yet the unfiltered list is 15 destinations then slash commands — no session rows. Typing "Alpha" does find "Alpha topic about vector search · Recent session · 70338e22… · Sessions", proving the source exists but is gated behind guessing a title. |
| J024 | friction | entry | power-user — wee | A conversation row in All conversations does not open the conversation — not by click, double-click, or Enter | With hash "#sessions": single click → "#sessions"; double click → "#sessions"; focus + Enter → "#sessions". The row is a selector; the opener is "Resume conversation" in the detail panel, which on the phone is at y=791 in an 844px viewport, beneath the bottom tab bar. |
| J038 | breaks-continuity | continuation | Researcher holdi | "Open this conversation" restores the thread but not the position: the matched sentence is scrolled far out of view. | Measured after clicking "Open this conversation" on the hit for "Kyoto trial" in an 11-turn thread: target line rect top = -2505 (bottom -2459), inViewport = false; transcript scroller scrollTop 3580 of scrollHeight 4322, clientHeight 701. No highlight element exists (highlight probe returned only `nav-item active`/`mobile-nav__tab active |
| J043 | lost-work | persistence | Researcher holdi | An explicitly remembered fact is silently destroyed by a page reload while Memory keeps claiming "Private · on-device". | Before reload: "Active profile memory 1 result … Kyoto trial: DCAP quote freshness window was 300 seconds per the 2019 spec note." After `reload()` with the identical query: "Active profile memory No matches". Header unchanged: "Private · on-device". No loss notice anywhere, unlike chat's "That conversation existed only in page memory and |
| J070 | lost-work | persistence | Developer using  | A reload destroys committed Git history and the route that lost it says nothing, while the route that lost far less has a dedicated sentence for exactly this. | Before reload the History listed 'docs: persist marker' (measured true). After reload: 'docs: persist marker' present false, 'PERSIST-MARKER' present false, History '1 — Initial browser workspace 20324f272e0f', Source Control '3 changes', and any 'did not survive' notice: (none). Contrast src/ui/app.tsx:2101, which I observed rendered ver |
| J077 | inaccessible | proof | mobile — someone | The transcript half of a conversation's runtime record is off-screen on a phone with no scroller and no pan | Measured after opening the "RUNTIME RECORD" disclosure at 390x844: gridCols "340px 270px"; panel "session-library-panel transcript" at x=391..661, offscreen:true, text "BOUNDED LOCAL MATERIALIZATION / Transcript / No user or assistant messages / Tool payloads and internal events are intentionally not rendered here."; ancestor chain shows  |
| J082 | trust-error | persistence | mobile — someone | After a successful vault-backed resume, the composer states the conversation did not survive the reload | One measurement, one page: {"hash":"#chat/4d439c5a-25c8-49ea-8fc0-e3f57076fb23", "title":"GE General · encrypted vault", "turnsInTranscript":2, "transcriptHasMyMessage":true, "durability":"Encrypted Local Device vault active · audited session resumed · local context fallback", "noticeText":"That conversation existed only in page memory an |
| J084 | breaks-continuity | response | mobile — someone | The blowout is caused by unbreakable provenance tokens in the resumed-session transcript markers | Leaf overflow measured after reload: "p.transcript-marker__provenance right=599 cut=209 txt='Event 2 · profile.active-conversation.selected · sha256:qfvu3DLU…'". /Users/chrisk/airship/src/ui/chat.css:423-429 styles `.transcript-marker__detail, .transcript-marker__provenance` with font-size and colour only — no overflow-wrap, word-break or |
| J085 | misleading-state | proof | mobile — someone | The false notice is itself truncated, so the claim cannot be read in full on a phone | "visibleNoticeText: full=\"That conversation existed only in page memory and did not survive the reload. This is a new conversation.\" width=584 clipped_at=390 (207px hidden)" — on screen it reads "…did not survive t" |
| J086 | lost-work | continuation | Returning — some | A returning person is never told their previous work did not survive; the return screen is indistinguishable from a first open. | Cold reopen of the same browser profile: url at close "#chat/abd5cf6f-1644-4cfe-a464-f54f174ffe23" with turns present; url on reopen "#chat/daeb82c4-e97c-4e7a-8ead-fb3becb0e0bd", composer notice = null, "All conversations" reads "1 conversation" / "General conversation / just now / Active / 2 events". |
| J087 | trust-error | proof | Returning — some | An empty, never-used conversation is decorated with completion and durability claims it cannot support. | #sessions detail for a 0-message conversation: "Structure passed", "Ready to resume", "0 receipts", "RUNTIME RECORD  Manifest pins and transcript · 0 messages". |
| J088 | misleading-state | persistence | Returning — some | The only warning that work is disposable is a 12.75px grey chip in the session bar, while the durability decision is a five-step ceremony on another route; the risk is stated at the same volume as the model name. | Session chip accessible name while working: "Session. Ephemeral · this page only. Secure hardware not checked · this session. No inference provider is connected, so no TEE evidence has been requested for this session. 4 facts." — computed fontSize 12.75px, color rgb(148, 156, 153). |
| J089 | friction | discovery | Returning — some | On a phone there is no recents affordance in the primary chrome at all; the conversation list lives behind a generic "More" sheet. | Visible bottom-bar controls at 390x844: "Idle \| Chat \| Workspace \| Trust \| More". "All conversations — Search and resume past chats" appears only inside the More sheet. |
| J090 | trust-error | recovery | Returning — some | Opening the conversation's own URL — the bookmark, the restored tab, the link you sent yourself — declares the conversation destroyed while the app restores it and lists it as resumable. | Cold open at "#chat/45b72a63-9a02-43e1-a1fb-5cfb2839a3ff" renders <p class="composer-notice" role="status"> at y=873: "That conversation existed only in page memory and did not survive the reload. This is a new conversation." while the topbar reads "Encrypted Local Device vault active · audited session resumed" and #sessions lists the sam |
| J092 | lost-work | persistence | Returning — some | The unsent composer draft is destroyed by reload and by browser restart even with the Vault active, silently. | Measured: before reload document.querySelector("textarea").value = "and one more thing I still need to check before Friday"; after reload = ""; after full browser restart = "". Same URL (#chat/c94a74f2…), same conversation restored, no notice. |
| J095 | friction | discovery | Returning — some | On every cold open the conversation list starts collapsed behind a chevron, so the returning person's most important object needs a disclosure click before it exists. | Measured on cold open: document.querySelector('button[aria-label="Expand recent conversations"]') present, i.e. railCollapsed = true; screenshot /Users/chrisk/airship/.ui-capture/journeys/RF-02-thursday-cold-open.png shows "Chat ›" with no rows. |
| J096 | breaks-continuity | continuation | Returning — some | An interrupted approval permanently quarantines the conversation, and the returning person is dropped into a brand-new empty conversation instead of their most recent healthy one. | Cold open after closing the tab with .approval-scrim present: url = "#chat/711816eb-8cb1-4f06-8f2e-45aac5ccfb5a", main text "General · encrypted vault ... 2 events ... The encrypted Local Device Vault is active. This new pinned session writes workspace files..." while a fully resumable sibling conversation (14 events) sat in the same list |
| J097 | recovery-failure | recovery | Returning — some | The only recovery action offered for a blocked conversation refuses silently; its refusal message renders above the top of the viewport with no way to scroll to it. | "Create fork" on the blocked conversation: conversations before=3, after=3, url unchanged (#sessions), no console/page error. The message "The observed source head did not pass the local journal audit (LOCAL_COMMAND_INCOMPLETE)." measured at y = -117, height 15, inViewport false, with document.body.scrollHeight = 900 in a 900px viewport. |
| J099 | misleading-state | discovery | Returning — some | Blocked and healthy conversations are visually identical in the recents rail — same title, same timestamp, no status marker — so the person cannot see which one holds the work that will not open. | Rail rows on return: "General · encrypted vault / No messages yet / 2:01 PM" and "General · encrypted vault / Airship: Airship is r... / 2:01 PM"; screenshot /Users/chrisk/airship/.ui-capture/journeys/ret-45-rail-after-interrupt.png. The "Needs review / Resume blocked" badges exist only inside the #sessions detail panel. |
| J100 | lost-work | proof | Returning — some | A quarantined conversation's own content is unreadable anywhere in the product; it can be renamed, proved and deleted, but never read. | Detail panel actions for session 7C63C91E: "Rename \| Proof \| Fork to continue \| Transcript cannot be replayed (disabled) \| Delete"; content appears only as "RUNTIME RECORD  Manifest pins and transcript · 2 messages" with no transcript view. |
| J101 | recovery-failure | recovery | failure-recovery | The error card advertises a recovery action that is disabled for every session without a Vault — i.e. the default state a newcomer is in when they hit their first failure. | On-screen: "Retry is available." DOM at the same moment: {"label":"Retry","disabled":true}, {"label":"Fork from here","disabled":true}. Source: src/ui/app.tsx:8763 `branchDisabled={!sessionLibrary \|\| !activeSessionRecord \|\| busy \|\| !entry.item.sourcePoint}` — an ephemeral session has no `activeSessionRecord`, so every branch/retry c |
| J106 | misleading-state | persistence | failure-recovery | After a reload with an encrypted Vault active, the composer declares the conversation lost while the rail lists it and the topbar says it was resumed. | Simultaneously on one screen (screenshot .ui-capture/journeys/fr-j22-03-recents.png, plain build, no lab namespace): composer — "That conversation existed only in page memory and did not survive the reload. This is a new conversation."; rail RECENT — "Lab title / You: PLAIN RUN: s… / 2:36 PM"; topbar — "Encrypted Local Device vault active |
| J107 | breaks-continuity | continuation | failure-recovery | The truthful resume record exists and is excellent, but is unreachable from the surface that announced the loss. | Three clicks away under All conversations: "Unfinished · Fork required", "Turn in progress TURN 4CB8ABE7…C782", "HISTORY INCOMPLETE — The most recent turn has no durable terminal event; fork before continuing.", "Continuing here requires a fork.", "Fork to continue". The chat route after the same reload offers only "Connect a model / Open |
| J115 | trust-error | proof | long-session — s | The event counter and digest read as a durable, chained journal while nothing is written to disk. | After 180 turns the chip reads "⌗ 1079" with aria-label "1079 recorded steps in conversation #5b583f0d. Open conversation details."; navigator.storage.estimate().usage measured 4,503,947 bytes before turn 1 and 4,503,947 bytes after; indexedDB.databases() returned only ["airship-local-device-keyring-v1@1"]. |
| J116 | lost-work | persistence | long-session — s | A refresh destroys an entire working day with no prior warning and no recovery path — only a past-tense explanation. | After reload: users=0, cards=0, header "General conversation … ⌗ 2 events", and the notice "That conversation existed only in page memory and did not survive the reload. This is a new conversation." #sessions afterwards: "1 conversation … 2 events". Reproduced 5/5 times, desktop and phone. |
| J117 | misleading-state | continuation | long-session — s | Nothing in the product is aware of how long the session has run or what it costs; latency stays flat while the tab grows, so there is no signal before the tab dies. | Turn latency p50 1,557 ms / p99 1,595 ms / max 1,630 ms across all 180 turns, first-10 average 1,387 ms vs last-10 average 1,580 ms. In that same session CDP JSHeapUsedSize after three forced GCs went 150.1 MB (t20) → 818.2 MB (t140) and retained-minus-live DOM nodes went 2,021 → 20,743 with live nodes frozen at 874. No banner, chip, or c |
| J119 | lost-work | persistence | long-session — s | Work the UI explicitly accepted and displayed is destroyed on reload with no accounting of what was lost. | Before reload the panel read "UP NEXT … 21 QUEUED" with 22 Edit buttons in the DOM. After reload the only notice on screen was "That conversation existed only in page memory and did not survive the reload. This is a new conversation." — no mention of the queue, no count, no drafts. |
| J122 | recovery-failure | recovery | long-session — s | A whole day of shell history and scrollback is unrecoverable by design with no export anywhere. | After reload the pane showed only "~/airship-node/airship-workspace ❯" and the footer read "Input history · 0"; the route's own text is "Reload loses them; processes also end." No copy/export/download control exists on the route. |
| J128 | friction | discovery | long-session — s | After eight hours you cannot tell which conversation you are in from the phone session bar. | Session bar title rendered as "Explain …" (and "queued …" in the queue run) at 390 px — the conversation is still named after the first message of the day and truncated to two words. |
| J129 | misleading-state | persistence | COMPLETENESS CRI | The second tab reports the first tab's live, open conversation as destroyed, on a screen that simultaneously says the first tab is open. | Same frame, tab 2: topbar "Open in another tab · page-memory state is not shared" (12.75px, [248,12,210,33]) and card "Your last visit was not kept / 1 conversation · 2 messages · last active Jul 31, 3:10 PM EDT. That work was held in page memory, which the browser releases when the page closes." Tab 1 was open and intact throughout. |
| J130 | breaks-continuity | continuation | COMPLETENESS CRI | A conversation's own address does not open that conversation from a second tab — the location is silently rewritten to the second tab's own empty conversation. | Set tab 2 hash to `#chat/bfe27533-3438-468e-bc27-7f9faf73ba33`; resulting URL `chat/85bb0736-204b-4369-9282-c0112de7d51d`; "MARKER PRESENT?: false" for the string "SECRET-MARKER-ALPHA". |
| J131 | lost-work | proof | COMPLETENESS CRI | The audit surface silos per tab: "All conversations" in tab 2 shows only tab 2's work and offers no indication that another tab holds more. | Tab 2 `#sessions`: "1 conversation" / "General conversation" / "2 events" / "CONVERSATION 870707F0…A59F" — tab 1's 11-event conversation is absent, and tab 1's own list never learns tab 2 wrote anything even after clicking "Refresh". |
| J134 | misleading-state | action | COMPLETENESS CRI | The primary control on the Capabilities page performs none of the action its label names. | Button "Activate in Chat →" on the card "Node.js · WebContainer … Available". After clicking: Capabilities still reads "3/6 runtimes ready · observed Jul 31, 3:03 PM EDT" (identical string before and after) and "No execution runtime has been asked to run anything in this page yet." |
| J135 | breaks-continuity | continuation | COMPLETENESS CRI | Pressing it abandons the conversation you are in, with no notice, no confirmation and no way back from that screen. | URL before `#chat/792a1381-2157-4666-b03a-6af5b2854bc2` ("PHONE-MARKER: keep this thread.", 11 events); URL after `#chat/a8ee5e19-2c68-4ff8-814a-49a8eda7f857`, empty state "Capability command is a new isolated conversation." Same result on desktop. |

## L3-vault-durability — 15 findings

| id | severity | link | persona | finding | evidence |
|---|---|---|---|---|---|
| J002 | friction | discovery | Novice — heard " | The persistent topbar alarm about the vault is inert text with no action attached, so a novice who reads a warning about a "recovery key" they do not understand cannot resolve it from where it appears. | "Local Device Vault needs a saved recovery key before first use" — measured `cursor: auto`, `clickableAncestor: false`, DOM chain SPAN.runtime-line__text → SPAN.runtime-line → DIV.topbar-actions → HEADER.topbar. After clicking it the URL was unchanged at `/?airshipLabNamespace=novice-durable-d#chat/17729e23-466a-4d26-bfd0-d6d499d3795e`. |
| J003 | inaccessible | discovery | Novice — heard " | The same vault alarm collapses to zero size on the phone, so it is announced to assistive technology but cannot be seen or acted on by anyone on mobile. | Measured at 390×844: `{"text":"Local Device Vault needs a saved recovery key before first use","x":0,"y":0,"w":0,"h":0,"onScreen":false}`. At 1440×900 the same node measures `{"w":345,"h":13,"onScreen":true}`. |
| J011 | misleading-state | continuation | Novice — heard " | Nothing on the chat surface warns that conversations are not being saved, before or after the fact; the word never appears where the person is typing. | Scanned every leaf text node on the chat view for /durable\|persist\|save this conversation\|keep\|survive\|adopt\|sync/i — result: `DURABILITY LANGUAGE ON SCREEN: []`. The only statement of the fact, "Ephemeral · this page only — This session journal exists only in page memory. Nothing is synced.", is inside a popover behind a 44 px unla |
| J020 | polish | completion | power-user — wee | The completion signal for a profile switch is visible for roughly a sixth of a second and is then overwritten by an unrelated storage warning | "Research cockpit started" sampled present at t=51/103/154ms and gone at t=205ms, replaced in the same slot by "Local Device Vault needs a saved recovery key before first use". |
| J056 | recovery-failure | recovery | privacy-first —  | The ceremony's durability sentence implies the recovery key alone survives losing the browser profile; it provably does not | src/ui/local-device-vault-setup.tsx:572 renders "Airship does not upload or persist this value. Losing both it and this browser profile means losing the Vault." — the word "both" states that losing only the profile is survivable. Driven: fresh browser profile + the correct key -> "The recovery key did not authenticate this Local Device Va |
| J057 | friction | discovery | privacy-first —  | The only artifact that can actually restore after profile loss cannot be created at the moment the operator is told what to save | During the ceremony the buttons inside .local-device-vault__ceremony are exactly ["Copy key", "Download recovery key", "Cancel"] — measured. "Download backup" ("PORTABLE CIPHERTEXT / Download encrypted backup — Includes the encrypted object inventory, never the recovery key") only renders after "Create encrypted Vault" succeeds, below the |
| J058 | misleading-state | permission | privacy-first —  | The custody warning states the key is unsaved and then permits the irreversible action it is warning about, with no confirm | With the Seal reading verbatim "Not copied or downloaded yet.", I clicked the acknowledgement checkbox and measured wasDisabled: false, nowChecked: true. The ceremony immediately advanced to data-state="acknowledged" and "The recovery value is no longer rendered." src/ui/local-device-vault-setup.tsx:589 has no disabled binding — onChange  |
| J091 | breaks-continuity | discovery | Returning — some | A conversation created by the Vault-attach path is named after the storage backend and never takes a name from its content, so Tuesday's memo is unfindable by title. | Rail row for the thread whose first message is "Draft the Q3 pricing memo intro paragraph." reads "General · encrypted vault"; the sibling created via New conversation reads "quick unrelated question about timezones". #sessions showed five separate rows all titled "General · encrypted vault". Source: /Users/chrisk/airship/src/ui/app.tsx:5 |
| J093 | misleading-state | proof | Returning — some | Two chips in the same composer row make opposite durability claims about the same conversation. | Simultaneously present accessible names: "Session. Encrypted · this device. ..." and "Credential posture. Local demo. No provider credential is held. Replies come from a deterministic local demo and this conversation's journal is page memory only." |
| J098 | misleading-state | response | Returning — some | The explanation for why the work did not come back is delivered as a 470-character single-line topbar chip containing raw internal identifiers, truncated to about 60 characters on screen. | Full chip text: "Encrypted Local Device vault active · “Set up the competitor file.” (session 7c63c91e) could not be replayed: The latest encrypted session no longer matches the adopted runtime. Structural history issues must be reviewed before this session can resume. LOCAL_COMMAND_INCOMPLETE: Client-only local command local-command-70aa |
| J110 | lost-work | persistence | failure-recovery | Turning on durability mid-session does not carry the conversation you were in; it silently swaps you to a new empty one. | Before: `#chat/7ec47231-a7cd-4532-a9eb-8a8a18199cfc` containing a completed turn. After Vault → "Create encrypted Vault" → Chat: `#chat/40ec5b12-c403-4664-a4cf-c6e49e4bfd56`, rail shows one entry "General · encrypted vault — No messages yet". The pre-Vault conversation is not listed and no sentence explains that pre-Vault work is not adop |
| J114 | trust-error | in-flight | long-session — s | The only durability warning in the product is evicted by the first turn's status and never comes back, replaced by an all-clear. | Topbar cold: "Local Device Vault needs a saved recovery key before first use". +0.6 s into turn 1: "Persisting turn intent". At +5 s, +15 s, +30 s, +60 s, +120 s, +180 s and after navigating #vault→#chat: "Local kernel ready". (src/ui/app.tsx:5167 sets "Persisting turn intent"; src/ui/app.tsx:3753 sets "Local kernel ready".) |
| J132 | trust-error | recovery | COMPLETENESS CRI | The remedy attached to the false loss notice is the irreversible Vault ceremony. | "Keep future conversations" → `#vault` → "Not set up yet", "Local Device setup required", "Create or recover the device key below." |
| J142 | inaccessible | response | COMPLETENESS CRI | The failure is silent to assistive technology and to anyone not looking at the pane: pressing Retry produces no state change, no announcement, no "still failing". | Live-region contents during the failure and across all three retries stayed "Local Device Vault needs a saved recovery key before first use" — unchanged. |
| J151 | lost-work | persistence | P0 root cause —  | The service-worker takeover reloads the document mid-journey and the page-memory conversation does not survive it | 1 navigation after the ceremony, 3 after loading #chat, `.pwa-update` present; a turn sent in that window is rendered and reported complete but absent after the reload |

## L4-connection — 14 findings

| id | severity | link | persona | finding | evidence |
|---|---|---|---|---|---|
| J006 | breaks-continuity | permission | Novice — heard " | The one provider the product is named around cannot be signed into at all, and says so in developer language; the "no account" promise silently becomes "get an account and mint an API key elsewhere, then re-paste it every session". | Verbatim on the Chutes card: "Chutes sign-in is not available in this build." and the OAuth tab reads "OAuth — Unavailable in this build". The only path offered is "Paste an API key to connect." with "Held only in page memory. Don't have one? Create a key at chutes.ai → API keys ↗". Confirmed non-persistent: on a second tab in the same na |
| J007 | trust-error | action | Novice — heard " | A syntactically-shaped but entirely fake key advances the flow into a green success banner, a lock chip reading like an established session, and a priced model picker — because the catalog reads are unauthenticated. The credential is not checked until a separate commit 10 s later. | With `cpk_notarealkey000000`: green banner "13 encrypted-inference candidates found.", chip "🔒 Chutes API key · direct session", model "Qwen/Qwen3-32B-TEE ✦ privacy-first recommendation", "AVAILABILITY hot", "INPUT / OUTPUT $0.104 / $0.416 USD per million tokens". Network during discovery: `GET https://llm.chutes.ai/v1/models auth=no`, `G |
| J008 | misleading-state | discovery | Novice — heard " | The connection chip counts provider integrations as "ready" when none of the five can be used without credential work the user must do outside Airship. | Chip text "No model connected · 5 ready", aria-label "No model connected · 5 ready. Connect one, or several at once." On the same page: Chutes needs a chutes.ai key, OpenAI/Anthropic/xAI each require ticking "I accept this browser-direct credential boundary" plus a pasted key, and Ollama/LM Studio show "Check this machine" with nothing ru |
| J059 | trust-error | in-flight | privacy-first —  | The screen states "Nothing has been sent yet." for 4,375 ms while the API key is actively in flight to two third-party hosts | Measured timeline from a single click: first off-origin request at t=9 ms (GET llm.chutes.ai/v1/models, GET api.chutes.ai/chutes/, GET api.chutes.ai/chutes/utilization); 200 responses at t=98 ms and t=116 ms; the visible line "Read as a Chutes personal key (cpk_). Nothing has been sent yet." was still rendered and visible at every 250 ms  |
| J060 | trust-error | response | privacy-first —  | A third-party image host is contacted for every discovered model, disclosed nowhere on a page that otherwise enumerates its own boundaries exhaustively | Measured: GET https://logos.chutes.ai/logos/da257fa7-0888-4197-a3bc-71d20ee30a65.webp at t=4247 ms, resourceType "image", from src/ui/model-picker.tsx:136 — `<img class="model-logo-img" src={`https://logos.chutes.ai/logos/${model.logoId}.webp`} alt="" />`. I scanned every sentence in the Connection main region for egress language; the onl |
| J072 | lost-work | in-flight | mobile — someone | The pasted API key is destroyed the moment "Discover models with key" is tapped, before any result exists | Measured on the same field 500ms apart: "1 BEFORE (typed a valid-shaped key): value: 'cpk_myrealkeypastedcarefully000000000', valueLen: 37" → "2 IN FLIGHT (0.5s): value: '', valueLen: 0" |
| J073 | trust-error | response | mobile — someone | The failure banner claims local state was kept on the same screen where the user's key was discarded | Banner verbatim: "Request failed. Local state was kept; no remote success is assumed." — measured field value at that instant: "" (0 chars) |
| J074 | trust-error | response | mobile — someone | After a network failure the field caption falsely diagnoses the user's key as the wrong kind of credential | Before submit: "Read as a Chutes personal key (cpk_). Nothing has been sent yet." After failure, same field: "Not read as a Chutes credential. Chutes personal keys start with cpk_." — the submitted key was "cpk_myrealkeypastedcarefully000000000" |
| J076 | friction | entry | mobile — someone | The Chutes key field is under the 16px threshold that makes iOS Safari zoom the layout viewport on focus and never zoom back | Computed style measured on the running page: "CHUTES KEY FIELD: {\"rect\":\"32,397 326x46\",\"fontSize\":\"15.9375px\",\"type\":\"password\"}"; sibling fields "Paste credential = 13.3333px" x3 |
| J112 | misleading-state | response | failure-recovery | A dropped connection mid-stream is reported as corrupt provider data, pointing the person at the wrong system to fix. | On screen: "Turn failed / The local inference endpoint returned malformed UTF-8 or stream framing." Browser console for the same event: `Failed to load resource: net::ERR_INCOMPLETE_CHUNKED_ENCODING`. Source: src/inference/local/openai-transport.ts:201, reached because a mid-body network abort surfaces as a `TypeError`, which that branch  |
| J145 | misleading-state | proof | COMPLETENESS CRI | "Account" is filed as a global destination beside Vault and Connection but can only ever describe one of the four providers it lists. | `#account`: rows "Chutes / Not connected", "OpenAI / Not connected", "Anthropic / Not connected", "xAI / Not connected"; single action "Connect Chutes"; metrics "AVAILABLE CHUTES BALANCE", "CHARGED THIS UTC MONTH", "TOKENS THIS UTC MONTH"; "Nothing is read from Chutes until you connect." |
| J146 | trust-error | discovery | COMPLETENESS CRI | The provider count disagrees between two routes one click apart. | `#connection`: "No model connected · 5 ready to connect". `#account`: four provider rows. |
| J148 | inaccessible | discovery | COMPLETENESS CRI | On a phone a whole provider row on the Account page is off-screen with no scroll affordance. | Measured at viewport width 390: "Not connected" elements at x 307..417 and 447..557; document overflowX = 0 (the container clips rather than scrolls the page). |
| J150 | misleading-state | recovery | failure-recovery | The send-failure notice does not retract when connectivity returns, so a reconnected person is still told remote inference is paused | Live-region text at the "BACK ONLINE" step: "Offline · remote inference paused; prompt preserved", while the separate connectivity banner had already cleared |

## L5-proof — 10 findings

| id | severity | link | persona | finding | evidence |
|---|---|---|---|---|---|
| J005 | friction | completion | Novice — heard " | Answering a first, ordinary question auto-opens a cryptographic claim stack the novice did not ask for, taking a fifth of the desktop viewport and adding thirteen controls. | Panel measured 310 px of a 1440 px viewport (22%). Verbatim contents: "CLAIM STACK / Verification / Asserted, not verified", "Turn receipt only — 0 verified · 1 asserted · 7 no evidence", "Protected CPU runtime — No Intel TDX verification is present.", "Protected accelerator — No GPU confidential-compute verification is present." Visible  |
| J048 | trust-error | completion | Researcher holdi | A local (slash-command) turn produces no receipt at all, while the transcript labels it "COMPLETED TURN" and Proof says no turn has completed. | Chat after `/read notes/retrieval.md`: two "COMPLETED TURN" badges and conversation chip "5 events #f831f12d". Proof → Receipt & journal, same session: "No evidence", "Evidence is recorded when a turn completes.", "Complete a turn to create the first local receipt.", "NO EVIDENCE 8", "No turn receipt yet." |
| J049 | trust-error | proof | Researcher holdi | The receipt records transport and request/response digests but no provenance: nothing names which source, chunk or revision grounded the answer. | Full technical receipt: "EVIDENCE not attached", "REQUEST DIGEST sha256:gJLDkZQveM_l6gFq1TUDnXcCtbS78VJe9WFZzXPA-Bw", "RESPONSE DIGEST sha256:ZBfzw5Q8NhN0MwDy-2ysh4O7iHWX_CBexbdYVWg4NI8" — no path, chunk id or content digest — against the Index's claim "Selected turn context is bounded and committed to the session journal with source dige |
| J050 | breaks-continuity | continuation | Researcher holdi | Proof is a one-way door: the deep link carries session, receipt and turn ids, but the view offers no way back to the turn and never names the conversation or the message. | After "Inspect evidence →" the URL is `#proof?session=0b1fea50-…&receipt=urn%3Aairship%3Areceipt%3A394b3bd0-…&turn=1c52c6d1-…&section=attestations`. Every enabled control in `main`: "Receipt & journal", "Attestation evidence", "Export status summary", the eight claim buttons, "Export raw verification bundle", "Copy safe summary", "Export  |
| J053 | trust-error | proof | privacy-first —  | The primary, visually-emphasized export ships a plaintext-recoverable digest of the conversation, which the product elsewhere names as a privacy hazard it deliberately omits | turn-Export-raw-verification-bundle.json contains "responseDigest": "sha256:ZBfzw5Q8NhN0MwDy-2ysh4O7iHWX_CBexbdYVWg4NI8". I recomputed base64url(sha256("Airship is running this turn entirely on your device with the deterministic demo provider. Try /write notes/hello.md followed by content, /read notes/hello.md, or /ls.")) and got the iden |
| J054 | trust-error | proof | privacy-first —  | The exported bundle asserts independent verifiability but the product has no control anywhere that emits the input required to verify it | dl-Export-raw-verification-bundle.json ends with "A journal audit commitment can be recomputed only with the corresponding immutable session journal." and carries "commitment": {"sequence": 11, "digest": "sha256:86LpBKbfw9aMD80YW2bKSj52sdnA56qI2d7LHMplix8"}. Neither export contains the journal events: the raw bundle has "endpointEvidence" |
| J055 | misleading-state | discovery | privacy-first —  | The same trust model is counted three different ways one click apart, and the session chip says a different noun to a screen reader than to the eye | Measured on one screen: topbar chip visible text "Browser / Edge runtime4 axes"; session chip visible text "Secure hardware not checked · this sessionNot checked4 claims" but its accessible name ends "...4 facts."; and Proof one click later renders "NO EVIDENCE 8", "CLAIM STACK", "No evidence 8" and the tab's own aria-label "Turn evidence |
| J061 | trust-error | proof | privacy-first —  | There is no surface anywhere in Airship that shows what has left the device | Proof audits the journal and receipts only — its exports are "Export raw verification bundle \| Copy safe summary \| Export safe summary \| Export session audit", and journalAudit counts events/turns/toolOperations/shellRecords with no network dimension. Searching every link and button in the mobile "More" command center for /network\|act |
| J069 | trust-error | proof | Developer using  | On a phone the Proof route tells a person who has just made a commit under two approvals that no evidence exists and instructs them to complete a turn. | Verbatim, 390×844, immediately after 'Commit staged' → 'Allow once' succeeded and History showed the commit: "Receipt & journal  No evidence  Evidence is recorded when a turn completes. Complete a turn to create the first local receipt.  VERIFIED 0  ASSERTED 0  NO EVIDENCE 8". |
| J127 | misleading-state | proof | long-session — s | The phone shows the confident evidence chip without the panel that qualifies it on desktop. | At 1440x900 the chip "⌗ 1079 events" sits beside a rail reading "Asserted, not verified … 0 verified · 1 asserted · 7 no evidence … This turn has no matching endpoint evidence." At 390x844 the same chip is present and that rail is absent from the rendered page text. |

## L6-memory — 10 findings

| id | severity | link | persona | finding | evidence |
|---|---|---|---|---|---|
| J039 | misleading-state | discovery | Researcher holdi | A term the product itself suggests as a search returns nothing, while the index on the same page says the file is indexed. | Chip aria-label: "Search memory for retrieval, from this page's workspace source". Clicking it yields "No memory matched “retrieval”" and "Nothing was hidden, filtered, or ranked away.", while the Index panel reads "INDEXED notes/retrieval.md text/markdown · 72 B · 1 chunk" and "Following “retrieval”0 index results". Root cause: chips are |
| J040 | trust-error | response | Researcher holdi | Bootstrap-embedding noise is presented as a confident result, with the disqualifying score hidden three disclosures deep. | Query "Kyoto" → "Workspace & sources 1 result" → "/workspace/README.md" with the full README printed. Its provenance reads "SCORES Dense 0.065 · Lexical 0.000 · Combined 0.046" and "72% deterministic dense score · 28% lexical overlap." The same happens for "zzqqxv". README.md contains no occurrence of "Kyoto". |
| J041 | friction | proof | Researcher holdi | The evidence layer a researcher needs — generation-pinned Search hits, content digests, index lineage — is behind a disclosure that is closed by default and stays closed when a query is entered. | `document.querySelectorAll('details')` on `#memory`: {"summary":"REVISION-BOUND LOCAL MATERIALIZATION Index health, candidates, hits, a…","open":false} both cold and after typing "retrieval". Only the deep link `#context` opens it. |
| J042 | friction | recovery | Researcher holdi | A long investigation has no search history and no way to browse what is remembered — Memory can only be interrogated one guess at a time. | "Nothing searched yet. Each term below is read from this page's own index, profile and graph; no search history is kept." Empty query shows only four suggestion chips; wildcard attempts return "No memory matched “*”" and "No memory matched “a”". There is no list view of the profile's memory records anywhere in the UI. |
| J046 | misleading-state | action | Researcher holdi | Remembering anything requires a hand-written JSON slash command, and the product's own help documents a syntax the parser rejects. | `/help` prints "/update-memory <action> [id] [content] [source] [--json <json>]". Entering it in that order — `/update-memory remember --json {…}` — returns "Unknown option: --json." and "FAILED TURN". The flag is only accepted as the first and only token (src/commands/registry.ts:317-326), while the usage string is generated with it trai |
| J047 | friction | entry | Researcher holdi | There is no surface that lists what the profile remembers, so a researcher cannot audit, edit, or prune the memory they are relying on. | The Memory route's only inputs are the search box and graph view-filters ("Filters never alter memory."). No add/edit/forget control exists; forgetting requires `/update-memory` with action "forget" and a record id the UI only exposes via a "Copy Record id" button inside a per-hit provenance disclosure. |
| J052 | friction | continuation | Researcher holdi | The relationship graph knows a term appears in a specific message but offers no way to open it. | Selecting node "kyoto trial" gives "1 occurrence across 1 source" and "4 RELATIONSHIPS / Ideas mentioned 1 shown · 1 total / MENTIONS user: /update-memory --js…". Clicking that relationship leaves `location.hash` at `#memory`; the message node's inspector offers only "Hide from view" plus further node hops — no "Open this conversation", w |
| J139 | recovery-failure | recovery | COMPLETENESS CRI | The product's only chunk-failure recovery verb issues no network request and can never succeed. | Instrumented: 3 clicks of "Retry loading Memory" with the route unblocked produced "NEW CHUNK REQUESTS AFTER RESTORE: 0". Card text unchanged after each: "The private Memory interface could not be loaded. No index or workspace state changed." |
| J140 | recovery-failure | continuation | COMPLETENESS CRI | Leaving the route and returning does not retry either, so the failure is permanent for the tab. | After `#chat` → `#memory` round trip: "The private Memory interface could not be loaded. No index or workspace state changed. Retry loading Memory". |
| J143 | misleading-state | discovery | COMPLETENESS CRI | A destination the command palette advertises with its own name, scope and keyboard chord opens the page the user is already on. | Palette entry "Memory indexMemory · Session scope · g x"; selecting it sets the hash to `#context`; `#context` renders h1 "Memory" with content identical to `#memory` ("Private · on-device", "Search every memory surface", "Federated client recall · Results across private scopes"). |

## L7-workspace — 20 findings

| id | severity | link | persona | finding | evidence |
|---|---|---|---|---|---|
| J016 | friction | continuation | power-user — wee | Keyboard focus inside the terminal silently swallows navigation chords with nothing on screen saying which keys the page still owns | After clicking the xterm surface, focus = `TEXTAREA.xterm-helper-textarea aria=Terminal input`; pressing `g` then `s` left `location.hash === "#terminal"`. Tab → "Terminal input" (consumed); Shift+Tab → "Close terminal tab" (escapes). The terminal's own help text explains git and durability but never focus: "Interactive jsh process runnin |
| J018 | misleading-state | continuation | power-user — wee | A terminal declares itself unusable and then keeps working | After a General → Research → General round trip, the General terminal buffer reads "Airship changed terminal workspace authority; this terminal requires restart." — and the very next line, typed after that warning, is "❯ echo STILL-ALIVE" followed by "STILL-ALIVE". A power user will read the sentence, watch it execute anyway, and stop bel |
| J019 | breaks-continuity | continuation | power-user — wee | Live work left behind in another profile is disclosed nowhere in the profile you are standing in | While in Research with General's PTY still alive (its tab still reads "Running Terminal 1" on return), the only global process reading is the rail's "0 Idle · No execution run is in flight. Peak 0 this page." A body-text scan for /another profile\|other profile/ returned "none". |
| J037 | trust-error | permission | keyboard-a11y —  | The most consequential facts in the request — the target path and the size delta — sit outside the dialog's accessible description, so entering the dialog announces the generic capability sentence and not the specific consequence. | aria-describedby resolves to 'Create or replace one UTF-8 file in the private virtual workspace.' while the write consequences live in a separate <section aria-label="Write consequence"> (approval-dock.tsx:81-87) holding Target / Change / New size / Size delta. The dock's visible text begins 'CAPABILITY REQUEST · CHANGE Allow write_file o |
| J045 | misleading-state | proof | Researcher holdi | "Open profile memory" claims to open the record and opens nothing; the backing file is invisible in the workspace. | Clicking it sets `location.hash` to `#editor`, topbar reads "Opened the active profile memory record in the editor", and the editor pane reads "Open a file from Explorer / Nothing is downloaded until you select it." Explorer shows "3 files" (docs/architecture.md, notes/retrieval.md, README.md) — `/workspace/.airship/memory.json`, named by |
| J062 | trust-error | permission | Developer using  | Every Git approval — the only irreversible actions in the workflow — renders as unattributable, even though the dialog is holding the file path. reviewGitOperation synthesises tool names as `git_${operation.kind}` (git_stage, git_commit); writeApprovalFacts only has a case for "git_change", so both fall through to the default NOT_DERIVABLE branch with an empty targets array. | Modal: "Allow git_stage once?" / "Target: Adapter-selected target" / "Change: Consequence not derivable — read the raw arguments below" / "New size: Not supplied" / "Size delta: Not supplied". Expanding 'Arguments shown to the approval policy' reveals: {"repositoryId":"airship-workspace","worktreeId":"main","paths":["README.md"],"expected |
| J063 | recovery-failure | recovery | Developer using  | After a bad save there is no discard/revert anywhere in Workspace, although the capability is implemented and works from a text field on another route. | Scan of buttons/summaries/menuitems for /discard\|revert\|restore\|undo\|reset\|checkout\|clean/ returned [] in Explorer, [] in Source Control and [] in the README.md '•••' menu; only "Switch checkout" exists, inside 'Advanced source controls'. Meanwhile 'git restore README.md' typed into the Terminal route's 'Browser Git' field returns " |
| J064 | misleading-state | response | Developer using  | The completion toast is absolutely positioned over the editor status strip and covers the very evidence it is announcing — the save verdict chip and revision hash. | Measured at 1440×900: .workbench-notice at x248→710, y794→842; .editor-strip at x552→1440, y819→858 — a 158px × 23px overlap directly on top of the 'Saved' chip and 'README.md'. At 390×844 the notice (x16→374, y665→724) sits wholly inside the strip (x0→390, y609→740) and buries the 'Reveal in Explorer', 'Wrap' and 'Save' buttons — only th |
| J065 | trust-error | action | Developer using  | One workspace is described by three mutually contradictory absolute paths in a single frame, and the one the product prints everywhere is the one that does not exist in the shell. | In the same screenshot: the tab status chip reads "Running /workspace", the prompt reads "~/airship-node/airship-workspace", and the Browser Git note reads "Runs against the browser-owned .git holding /workspace". Typing `pwd` returns "/home/airship-node/airship-workspace". Typing `ls /workspace` returns "ls: cannot access '/workspace': N |
| J066 | misleading-state | persistence | Developer using  | The shell and the Explorer are two copies of the workspace that diverge silently in both directions, with nothing on screen indicating drift. | Editor→shell: saved README.md with 'EDITOR-SIDE-MARKER' (strip confirmed 'Saved … rev 4a73a97 866 B'); a live shell's `cat README.md` did not contain it (measured false), and still did not after navigating away and back; after clicking 'Reconcile workspace' it did (measured true). Shell→Explorer: `echo 'from the terminal' > from-terminal. |
| J067 | friction | discovery | Developer using  | `git` in the terminal of a Git-capable product dead-ends in a raw shell error instead of routing to the bridge that is on the same screen. | Terminal scrollback: "❯ git status" → "jsh: command not found: git". The Browser Git field directly below already explains 'not inside the jsh process, which has no git binary' — but only to someone who reads it before typing, and the field's placeholder is literally 'git status'. |
| J068 | trust-error | proof | Developer using  | The exported session audit reports zero tool operations and declares itself complete after two human-approved, write-effect Git operations that changed the repository. The human.intent.reviewed events are journaled and validated but counted by no field in the counts block. | /tmp/claude/airship-session-audit-42518d6f.json, exported immediately after staging and committing README.md: "checks": {"schema":true,"chain":true,"manifest":true,"protocol":true,"receiptBindings":true,"complete":true}, "counts": {"events":4,"turns":0,"toolOperations":0,"terminalToolOperations":0,"localCommands":0,...}, "findings": []. N |
| J071 | breaks-continuity | continuation | Developer using  | Chat has no continuation from the work: the header event counter never moves for Workspace effects and the empty state still pitches the workspace as if nothing had happened. | Chat header read '2 events' at cold open and '2 events' after staging and committing (Proof's journal moved 2 → 4 over the same interval). After the commit, the chat empty state still offers "Browse the workspace — Files, the editor and browser-owned Git" and "Open a terminal — Real processes in this tab, no account", with no mention of t |
| J078 | inaccessible | recovery | mobile — someone | The control that closes a terminal tab is 32px past the right edge of a 390px screen and cannot be reached | "CLOSE BUTTON before pan: {\"x\":378,\"right\":422,\"cw\":390,\"offRight\":32,\"nearestHScroller\":null,\"barOvX\":\"visible\"}" and after a full-width CDP touch pan: identical. Same control at 1440x900: "1338..1403 inside" |
| J079 | misleading-state | response | mobile — someone | Terminal status chips are truncated mid-word so the page-memory caveat cannot be read on a phone | Rendered chip reads "Metadata · Ephemeral · th" (measured right=442 against clientWidth 390); "Profile general" measured right=547, i.e. 157px off-screen |
| J080 | friction | entry | mobile — someone | Workspace and Browser-Git text inputs are under the 16px iOS focus-zoom threshold | Measured computed font sizes on the running page: "Filter workspace files by path = 12.75px" (#workspace), "git status = 12.75px" (#terminal Browser Git command field) |
| J081 | friction | discovery | mobile — someone | A whole agent profile is invisible on a phone with no affordance that the strip scrolls | At rest on #profiles: "GE General… x=14..284 ok \| RE Research… x=291..561 CLIPPED \| DE Developer… x=568..838 FULLY OFFSCREEN". It is reachable — a CDP touch pan brought it to x=99 — but nothing on screen says so |
| J123 | misleading-state | continuation | long-session — s | A freshly-spawned shell inherits the identity of the one that died, so nothing on screen distinguishes continuity from restart. | After the reload the tab still reads "Terminal 1" with a green check and the pane header reads "Running /workspace", over an empty scrollback and "Input history · 0". |
| J124 | misleading-state | proof | long-session — s | You cannot see what you left running: the shell has no job control or process listing, and background launches give no confirmation. | "jsh: command not found: jobs", "jsh: command not found: yes", "head: -20: No such file or directory"; `ping -c 1000 127.0.0.1 … &` produced no output and no entry anywhere in the UI. |
| J136 | misleading-state | persistence | COMPLETENESS CRI | The composer caption keeps asserting a prefilled command after the command is gone, in a different conversation, and in a different profile. | "New profile-scoped conversation · capability command prefilled" still rendered with textarea value "" in `#chat/266cc04e` (new conversation) and in `#chat/16d5c8cb` under the Research profile. |

## L8-shell-a11y — 18 findings

| id | severity | link | persona | finding | evidence |
|---|---|---|---|---|---|
| J004 | misleading-state | response | Novice — heard " | On the phone, once the empty-state copy scrolls away, nothing visible tells the user they are talking to a demo. The two chips that carry that fact are reduced to unlabelled glyphs. | Measured after sending: the chip with aria-label "Session model. Demo · local. …" is 89 px wide at 1440×900 and 44 px at 390×844; the chip with aria-label "Credential posture. Local demo. …" is 84 px vs 44 px. Screenshot /Users/chrisk/airship/.ui-capture/journeys/novam-03.png shows the header row rendering only "⬡ ✧ ○ #" and the composer  |
| J012 | friction | discovery | power-user — wee | Nothing on screen teaches ⌘K; the chord lives only in a hover tooltip | On a fully loaded chat route, `document.body.innerText.includes("⌘K")` === false and `.includes("⌘\\")` === false. The only carrier is `title="Command palette · ⌘K"` on the topbar button (src/ui/app.tsx:8509) — invisible to touch and to anyone who does not hover a bare ⌘ glyph. |
| J013 | friction | discovery | power-user — wee | There is no shortcut sheet anywhere: no `?`, no F1, no Preferences section, and the palette cannot even find the word | `?`, `F1`, `Shift+/` each produced dialogs `[]`. Preferences dialog innerText tested against /keyboard\|shortcut\|chord/ → false (its sections are "Active profile approvals / Color mode / Type scale / Density / Corners / Tool steps / Body font / STORAGE Durability"). Palette queries "shortcut", "keyboard" and "chord" each returned "No mat |
| J017 | friction | entry | power-user — wee | There is no keyboard path to the thing a multi-profile user does most | Tabbing from the transcript, the "Agent profile" control is the 24th tab stop (order ended: … "Skip to conversation", "Skip to composer", "Open session", "Runtime trust for this browser tab…", "Connect a model", "Open command palette", "Open Preferences", "Open proof", "Agent profile"). NAVIGATION_JUMPS (src/ui/platform-shell.tsx:219) bin |
| J026 | inaccessible | entry | keyboard-a11y —  | The composer — the app's primary control and the landing target of the skip link, every send, and every dialog Escape — has no perceivable focus indicator at either width. | Computed on the focused textarea: `outline: "none 0px"`, `boxShadow: "none"`, `el.matches(':focus-visible') === true`. The only change is the `.composer` wrapper's 1px border going from `oklab(0.708221 0.0177785 0.0946424 / 0.45)` to `color(srgb 0.756863 0.603922 0.345098 / 0.45)`. Measured pixel diff blurred-vs-focused over the composer: |
| J027 | recovery-failure | recovery | keyboard-a11y —  | Escape from the command palette or Preferences does not restore focus to the opener; it drops to <body> and is then captured by the composer, 21 Shift+Tabs from where the user was. | rAF focus trace after Escape: `t=0ms input:` → `t=9ms body:Skip to conversation Skip to compo` → `t=26ms textarea:Message Airship`. Opener was `"Open command palette"`. Measured return cost: `Shift+Tab presses to get back to "Open command palette": 21`. Cause is visible in the code: src/ui/platform-shell.tsx:145 captures `restore.current  |
| J028 | friction | discovery | keyboard-a11y —  | There is no skip link to navigation, and on the phone navigation is the last thing in the document — so reaching Vault, Memory, Account or Proof from a cold phone load costs 25 Tab presses through the entire page. | Only two skip targets exist: tab stop 1 'Skip to conversation', tab stop 2 'Skip to composer'. Phone chain measured: `mobile nav entered at tab stop 22: "Chat"`, `reached "More" at tab stop 25` of 25 total stops. The 11 destinations behind it (Editor, Terminal, Memory, Profiles, Skills, Capabilities, Vault, Connection, Account, Settings,  |
| J032 | inaccessible | response | keyboard-a11y —  | Local slash-command failures are announced to nothing: no live-region change, no role=alert, anywhere on the page. | Three consecutive keyboard-only runs, each with an rAF live-region watcher installed before the keystrokes: `cmd="/ls" announced (0): []  role=alert nodes on the page: []`; `cmd="/write notes/hello.md hello from a keyboard only user" announced (0): []  role=alert nodes on the page: []`; `cmd="/nonsense-command" announced (0): []  role=ale |
| J035 | trust-error | persistence | keyboard-a11y —  | The outcome of a security decision is never announced. Denial and allow both resolve into utterances that say nothing about the effect. | Denial — announced: `[{"role":"status","live":"polite","v":"Airship’s turn ended."}]`; on screen: 'Permission denied for local /write-file. No tool effect ran, and nothing was sent to the model.' Allow — announced: `[{"role":"status","live":"polite","v":"Local command complete; no model request made"}]`; on screen: 'Wrote /workspace/notes |
| J036 | recovery-failure | permission | keyboard-a11y —  | The five-minute decision deadline is invisible to assistive technology, and the request is silently withdrawn when it elapses. | src/ui/approval-dock.tsx:67 renders it as role="timer", whose implicit aria-live is off — my watcher logged 'Decision expires in 05:00 / 04:59 / 04:58 …' once per second with `role=timer, live=null`, i.e. text churn no screen reader will speak. It is also excluded from the dialog's accessible description: aria-describedby="approval-descri |
| J044 | lost-work | permission | Researcher holdi | The approval modal focuses the destructive choice, so Enter or Escape — both reflexes in a chat surface — throw the note away with no retry. | activeElement in the dialog = {"tag":"BUTTON","text":"Deny","autofocus":false}. Escape produced "Permission denied for local /update-memory. No tool effect ran, and nothing was sent to the model." The denied assistant turn exposes only "Copy" and "Fork from here"; no Retry control exists on it. |
| J109 | inaccessible | recovery | failure-recovery | On a phone the one sentence that explains lost work is clipped by the viewport and cannot be read or scrolled to. | Measured at 390×844: notice element width 584 px, right edge x=597, viewport 390; computed `white-space: nowrap`, `text-overflow: ellipsis` (never engages because clientWidth === scrollWidth === 584). document.documentElement.scrollWidth 480 vs clientWidth 390. On screen: "That conversation existed only in page memory and did not survive  |
| J120 | friction | discovery | long-session — s | A large backlog is only 15% visible on a phone, inside a nested scroller with no affordance. | Measured `.composer-queue` clientHeight 208 / scrollHeight 1353 at 390x844 with 22 rows; 5 rows fully within the viewport; no scrollbar, no "show all", and the header count "21 QUEUED" is the only indication the other 17 exist. |
| J133 | inaccessible | discovery | COMPLETENESS CRI | On a phone the peer-tab disclosure collides with the app header instead of being read. | Measured rect [52,0,71,99], font-size 12.75px, six lines, in a topbar whose session bar starts at y=52; viewport width 390. |
| J138 | friction | completion | COMPLETENESS CRI | The Skills route accepts a change it has just told you is inert for the conversation you are in, and offers no action to make it live. | "Set global skill defaults. Enabled instructions are pinned into the next conversation manifest." → toggle → "EFFECTIVE SET 4 of 6"; back in chat the chip still reads "3 skills" and the panel reads "This immutable set composed the conversation prompt. Later Skill changes apply only to a new conversation." The only controls on the Skills r |
| J144 | friction | entry | COMPLETENESS CRI | The chord the palette teaches for that destination does nothing from where a person actually lands. | Cold load, pressed `g` then `x`: hash remained `#chat/89e1f36c-fd20-4e16-ab06-e23325c2668e`. |
| J147 | friction | discovery | COMPLETENESS CRI | Skills and Capabilities — the two routes that decide what the agent is and what it can run — have no presence in the desktop navigation rail at all. | Desktop rail rows enumerated: Chat, Workspace (Editor, Terminal), Memory, Proof, GLOBAL: Vault, Connection, Account. src/ui/navigation-model.ts RAIL_LAYOUT contains no `skills` or `capabilities` row; they are nested under `profiles`, which is itself not a rail row. |
| J152 | inaccessible | action | P0 root cause —  | The update banner intercepts pointer events over the composer | Playwright: `<div role="status" class="pwa-update">…</div> intercepts pointer events` while clicking Send, 58 retries |
